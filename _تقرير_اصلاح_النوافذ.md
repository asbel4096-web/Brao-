# تقرير: إصلاح تغطية شريط التنقّل لنوافذ القصص والنوافذ المشابهة

استبدل الملفات الخمسة كما هي عبر GitHub.

## السبب الجذري
نافذة "إضافة قصة جديدة" في صفحة المعرض كانت **z-50 = نفس z شريط التنقّل**.
ولأن الشريط يُرسم على مستوى الصفحة الأساسي، كان يظهر **فوق** النافذة ويقطع حقل رفع الصورة.
كما أن النافذة كانت **متمركزة عمودياً بلا تمرير ولا safe-area** فتُقطع على الشاشات الصغيرة.

## الملفات المعدّلة

### 1) `components/dealer-edit/stories-edit-tab.tsx` (الإصلاح الأساسي)
أعيد بناء `CreateStoryModal` بالكامل:
- ✅ **z-[60]** (أعلى من الشريط z-50، ودون التنبيهات حتى تبقى رسائل الخطأ ظاهرة فوقها).
- ✅ **Bottom Sheet متجاوب**: على الهاتف يلتصق بالأسفل ويرتفع حتى `92vh`؛ على الشاشات الكبيرة نافذة مركزية.
- ✅ **تمرير كامل** للجسم: `overflow-y-auto` + `maxHeight: calc(92vh - 56px)`.
- ✅ **Safe Area + مساحة سفلية**: `paddingBottom: calc(120px + env(safe-area-inset-bottom))` — لا يغطّي الشريط أي عنصر.
- ✅ **رأس ثابت** فيه العنوان وزر الإغلاق.
- ✅ **مساحة رفع صورة كبيرة**: عرض كامل، ارتفاع `min-h-[200px]` (≥180)، أيقونة أكبر + تلميح الصيغ والحجم.
- ✅ أزرار أكبر (h-11) وسهلة اللمس.

### 2) `components/stories/story-create-modal.tsx` (قصص الرئيسية)
- ✅ إضافة `paddingBottom: calc(24px + env(safe-area-inset-bottom))` لمنطقة التمرير، حتى لا يلامس زر النشر مؤشّر الـHome على آيفون.
- (هذه النافذة z-[130] أصلاً، فخلفيتها تخفي الشريط — لا تغطية أصلاً.)

### 3-5) النوافذ المشابهة (#8) — رفع z فقط
كانت أيضاً على **z-50** (تعادل مع الشريط)، رفعتها إلى **z-[60]**:
- `components/wallet/wallet-sheet.tsx` (المحفظة)
- `components/wallet/transfer-sheet.tsx` (تحويل الرصيد)
- `components/report/report-dialog.tsx` (الإبلاغ)

## مراجعة بقية النوافذ (لا مشكلة فيها)
- **نوافذ الترقية والشحن** (`boost-sheet`, `topup-sheet`): z-[60] أصلاً، خلفيتها تخفي الشريط. ✅
- **إضافة إعلان / تعديل إعلان**: صفحات كاملة (وليست نوافذ) وتحترم safe-area أصلاً. ✅
- **شريط التنقّل** نفسه يحترم `env(safe-area-inset-bottom)` أصلاً. ✅

## تراتب الطبقات النهائي (z-index)
```
شريط التنقّل           50
النوافذ (قصص المعرض/المحفظة/التحويل/الإبلاغ/الترقية/الشحن)   60
التنبيهات (Toast)     100
نوافذ التأكيد          110
نافذة قصص الرئيسية      130
عارض القصص            140
```
نتيجة: لا يوجد أي عنصر مخفي خلف شريط التنقّل، والتنبيهات/التأكيد تبقى فوق النوافذ.

## Responsive (#6, #7)
- Mobile-first: Bottom Sheet على الهاتف (يصل 92vh) → على الشاشات القصيرة (آيفون 12/13/14/15، أندرويد صغير) يملأ معظم الشاشة ويُمرَّر بالكامل بدل bottom sheet صغيرة.
- على sm+ يتحوّل لنافذة مركزية.
- مساحة الرفع عرض كامل تتكيّف مع كل العروض.

## ملاحظة عن البناء
البيئة هنا بدون إنترنت/`node_modules`، فما قدرت أشغّل build/TS/ESLint ولا ألتقط Screenshots.
راجعت يدوياً: توازن الأقواس في النافذة المعاد بناؤها (144/144)، وتراتب الـz-index. شغّل `npm run build` على Vercel للتأكيد.
