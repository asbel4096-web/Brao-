# إضافة نوع "عاجل" (Urgent) — المرحلة 5

نظام الترقية عندك كان فيه أصلاً 3 أنواع: **مميز** (أزرق) · **ممول** (أخضر، يثبت أعلى البحث) · **VIP** (ذهبي).
هذا التحديث يضيف النوع الرابع الناقص: **عاجل** 🔴 — وسم أحمر يلفت الانتباه للبيع السريع.

> **مهم:** كل التغييرات *إضافية فقط*. الإعلانات القديمة والأنظمة الحالية (المحفظة، البحث، الأدمن) تشتغل زي ما هي بدون أي كسر. النوع "مثبت/Pinned" الموجود بالنموذج = هو "ممول" عندك (نفس الوظيفة).

## آلية العمل
- نوع "عاجل" **إضافة مستقلة** (add-on)، مش مرتبة في سُلّم الأولوية.
- يُضاف فوق أي باقة (مميز/ممول/VIP) أو بمفرده.
- السعر: **30 BC**، المدة: **3 أيام** (أرخص باقة).
- يُخزَّن في حقل جديد على الإعلان: `urgentUntil` (يُكتب من السيرفر فقط عبر Admin SDK).
- ترتيب البحث ما تغيّرش: VIP > ممول > مميز > عادي. "عاجل" شارة فقط.

## الملفات المعدَّلة (12 ملف — استبدلها كما هي عبر GitHub)
1. `lib/wallet/types.ts` — سعر `PROMO_URGENT = 30`.
2. `lib/wallet/boost.ts` — نوع `urgent` + الحقل + الدوال (`isUrgentNow`, `urgentDaysRemaining`).
3. `app/api/wallet/boost/purchase/route.ts` — شراء "عاجل" بخصم المحفظة + تمديد تراكمي.
4. `components/listing-card.tsx` — شارة عاجل حمراء على البطاقة.
5. `components/compact-listing-card.tsx` — شارة عاجل في الصفوف الأفقية.
6. `components/my-listing-card.tsx` — تمرير حالة عاجل/VIP لزر التعزيز.
7. `components/wallet/boost-sheet.tsx` — بطاقة شراء "عاجل" + الحالة.
8. `app/(public)/listings/[id]/page.tsx` — شارة عاجل داخل صفحة الإعلان.
9. `app/admin/boosts/page.tsx` — تبويب وإحصائية "عاجل" بلوحة الأدمن.
10. `hooks/wallet/use-active-boosts.ts` — قراءة الإعلانات العاجلة النشطة.
11. `app/api/cron/boosts-cleanup/route.ts` — تنظيف "عاجل" المنتهي تلقائياً.
12. `app/api/admin/boosts/cleanup/route.ts` — تنظيف يدوي من الأدمن.

## بعد الرفع
1. ارفع الملفات الـ12 على GitHub (نفس المسارات) → Vercel يبني تلقائياً.
2. تأكد أن feature flag باسم **`wallet`** مفعَّل (وثيقة `featureFlags/wallet` = `{ enabled: true }`)، لأن "عاجل" يعتمد عليه.
3. (اختياري) لو ظهرت رسالة index في Firebase Console عند فتح تبويب "عاجل" بالأدمن، اضغط الرابط في الرسالة لإنشاء index الحقل الواحد `urgentUntil` تلقائياً.

## لم أستطع تشغيله هنا
هذه البيئة بدون إنترنت و بدون `node_modules`، فما قدرت أثبّت الحزم ولا أشغّل البناء. شغّل هذه محلياً أو اعتمد على بناء Vercel:
```
npm install
npm run type-check   # tsc --noEmit
npm run lint
npm run build
```
راجعت الأنواع والـ JSX يدوياً بدقة (توازن الأقواس + اكتمال الأنواع).
