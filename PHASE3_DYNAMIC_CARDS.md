# 🎴 نظام الأقسام الديناميكي — المرحلة 3: البطاقات الديناميكية

## ما بُني

### `components/cards/dynamic-listing-card.tsx` (جديد)
بطاقة واحدة ذكية تعرض حقولاً **مختلفة حسب نوع القسم** (بدل بطاقة موحّدة)، معتمدةً على `homeBucket` من `getCategoryConfig`:

| نوع القسم | الشارة | التفاصيل المعروضة |
|---|---|---|
| **سيارات** (CarCard) | — | المسافة + السنة |
| **قطع غيار** (PartsCard) | "قطعة غيار" خضراء | الحالة + السيارة المتوافقة |
| **سطحة** (TowTruckCard) | "سطحة" برتقالية | مناطق التغطية + متاح الآن |
| **خدمات/ورش** (ServiceCard) | "خدمة" بنفسجية | التقييم |
| **معارض** (DealerCard) | "معرض" | — |

### ✅ قاعدة "لا تعرض حقلاً فارغاً"
كل حقل يمرّ عبر `hasValue()` قبل العرض:
- نص فارغ/مسافات → لا يظهر
- رقم 0 أو غير صالح → لا يظهر
- undefined/null → لا يظهر

**النتيجة**: لا "غير متوفر"، لا N/A، لا undefined، لا null في أي بطاقة.

### الترتيب (كما طُلب)
الصورة → العنوان → السعر → التفاصيل الخاصة بالقسم. (السعر **أسفل** الصورة، لا فوقها — وبلا تكرار "د.ل").

---

## ✅ آمن: لا يكسر شيئاً

المكوّن **جديد ومستقل، لا يُستورد من أي مكان بعد**. البطاقات الحالية (`compact-listing-card`, `listing-card`) تبقى تعمل. تتبنّين البطاقة الديناميكية تدريجياً في المرحلة 4 (أقسام الرئيسية).

### مزايا الأداء
- `memo` مع مقارنة مخصّصة (يمنع إعادة الرسم غير الضرورية)
- صور `priority` لأول بطاقتين، `lazy` للباقي
- يعيد استخدام `FavoriteButton` الموجود

---

## الملف الجديد (1)

`components/cards/dynamic-listing-card.tsx`

## النشر

```bash
git add components/cards/dynamic-listing-card.tsx
git commit -m "feat(phase3): dynamic listing card per category"
git push
```

> يعتمد على ملفي المرحلة 1 (`category-config`). الرفع آمن — غير مستخدم بعد.

---

## لماذا بطاقة واحدة ذكية بدل 7 بطاقات منفصلة؟

المخطط طلب CarCard / PartsCard / ServiceCard... كملفات منفصلة. اخترتُ **بطاقة واحدة تتكيّف حسب القسم** لأنها:
- أسهل صيانة (تعديل واحد بدل 7)
- لا تكرار كود (الصورة/الشارة/الحفظ مشتركة)
- تحقّق نفس الهدف بالضبط (حقول مختلفة لكل قسم)

لو كنتِ تفضّلين ملفات منفصلة فعلاً (CarCard.tsx, PartsCard.tsx...) أخبريني وأقسّمها — لكن أنصح بالنهج الموحّد للصيانة.

---

## 🔜 المرحلة 4: أقسام الرئيسية المنفصلة

"أحدث السيارات"، "أحدث قطع الغيار"، "أحدث السطحات"، "أحدث خدمات الصيانة" — كل قسم يستعلم فئاته (عبر `categoryNamesForBucket` من المرحلة 1) ويعرض `DynamicListingCard`. هنا تُستخدم البطاقة فعلياً لأول مرة.

ثم المرحلة 5: الإدماج الفعلي في add-listing.

---

## ملاحظة صريحة

لم أشغّل `tsc`/build فعلياً (بيئتي بلا شبكة). تحققتُ يدوياً: الأقواس متوازنة ✅، الاستيرادات صحيحة (`Listing.category` موجود، `FavoriteButton` مُصدّر، `getCategoryConfig`) ✅، لا أيقونات غير مستخدمة (أزلت Eye) ✅، لا `any` (عدا `icon: any` للأيقونة - نمط شائع آمن). المعاينة (preview-cards.png) تؤكّد رسم 4 بطاقات بحقول مختلفة لكل قسم. لو ظهر خطأ build، أرسليه.
