# تقرير: قسم "استكشف جميع الأقسام" في الصفحة الرئيسية

استبدل/أضف الملفات الثلاثة عبر GitHub.

## الهدف
إظهار كل أقسام المنصة (17 قسماً) في الصفحة الرئيسية بدل 4 أقسام فقط — لتقليل النقرات وزيادة الوصول للإعلانات.

## ملفات جديدة

### `components/explore-categories.tsx` (جديد)
- **ديناميكي 100%**: يقرأ الأقسام تلقائياً من `lib/categories` (مصدر الحقيقة الواحد الذي يبني عليه `category-config`). **أي قسم جديد تضيفه هناك يظهر هنا فوراً** دون لمس هذا الملف — لا شيء مكتوب يدوياً.
- بطاقات صغيرة أنيقة، **صفّان أفقيان** مع تمرير (`grid-flow-col grid-rows-2` + `overflow-x-auto`).
- كل بطاقة: **أيقونة** (من اسم الأيقونة المخزّن) + **اسم القسم** + **عدد الإعلانات الحيّ** + **لون حسب المجموعة** (مركبات=أزرق، قطع=برتقالي، خدمات=أخضر، خاص=وردي).
- كل بطاقة تفتح صفحة القسم مباشرة: `/listings?category={slug}`.
- زر "عرض الكل" → صفحة `/categories`.

### `hooks/useCategoryCounts.ts` (جديد)
- يحسب عدد الإعلانات المعتمدة لكل قسم عبر `getCountFromServer` (عدّ خادمي كفؤ لا يقرأ المستندات).
- يَعُدّ بـ `category == الاسم العربي` + `status == approved` (نفس تخزين Firestore، يشمل الخدمات والساحبات لأنها كلها في مجموعة `listings`).
- **كاش جلسة 30 دقيقة** (نفس نمط `useBrandCounts` المُجرّب) لتقليل القراءات.
- أثناء التحميل تظهر شرائح تحميل صغيرة بدل الأرقام (لا قفزة تخطيط).

## ملف معدّل

### `app/page.tsx`
أُدرج `<ExploreCategories />` **بعد القصص مباشرة وقبل البانر**. الترتيب الآن:
```
Search → Stories → استكشف الأقسام → Hero Banner → (تعطّلت سيارتك/الماركات/المميزة/الأحدث...) → CTA
```
> هذا الملف يشمل تعديلات الجولة السابقة (البانر بعد القصص) — فهو الأحدث، استبدل به السابق.

## ملاحظات
- **لم أحذف** الأقسام الحالية الأخرى (الماركات، الإحصائيات، المميزة، الأكثر حفظاً...) — طلبك كان *إضافة* قسم الاستكشاف. لو تبي أعيد ترتيب/أحذف أقساماً لتطابق قائمتك المثالية بالضبط، قول لي.
- الأقسام السبعة عشر تأتي من `lib/categories` (سيارات، حافلات، شاحنات، قطع غيار سيارات/شاحنات/كهربائية/مستعملة، كماليات، زيوت، إطارات، ميكانيكي متنقل، ساحبة، سمكرة، ورش، كهربائي، حوادث، خدمات وتقارير).

## Responsive
- صفّان أفقيان على كل المقاسات، تمرير سلس، بطاقات `w-[164px]` لا تنكسر على 320/375/390/430px.
- حافة-لحافة على الموبايل (`-mx-4 px-4`) وداخل الـcontainer على الديسكتوب.

## البناء
البيئة هنا بدون إنترنت/`node_modules`، فما شغّلت build/TS/ESLint. راجعت يدوياً: توازن الأقواس (المكوّن 35/35، الـhook 23/23، الصفحة 47/47)، والاستيرادات (`formatNumber`, `categories`, `useCategoryCounts`). شغّل `npm run build` على Vercel للتأكيد.
