# 🏢 صفحة المعارض — المرحلة 3: واجهة تحرير ساعات العمل

أكملتُ الفجوة من المرحلة 2: المعرض الآن **يستطيع إدخال** ساعات عمله (كان العرض جاهزاً لكن لا إدخال).

## ما أضفته (`info-edit-tab.tsx`)

### واجهة تحرير ساعات العمل
لكل يوم من السبت للجمعة:
- **اسم اليوم** + حقلا وقت (فتح – إغلاق) باستخدام `<input type="time">` (يدعم كل الأجهزة)
- **زر إغلاق/فتح**: لو اليوم مغلق، يظهر "مغلق" أحمر مع زر "فتح"

### تحفظ في `workingHours` (الحقل الصحيح)
بنفس بنية UserProfile: `Record<day, {open, close} | "closed">`. القيم الافتراضية: 09:00–18:00.

### + تصحيح المرحلة 2 محفوظ
أبقيتُ تصحيح حقل الواتساب (`whatsapp` بدل `whatsappNumber`) — لأن هذا الـZIP لم يكن محدّثاً به. الآن الحفظ يشمل: whatsapp الصحيح + workingHours.

> **مهم**: تحققتُ أن `whatsapp` و`workingHours` **كلاهما في allowlist قاعدة الأمان** (`hasOnly`) → التحديث سينجح.

---

## الآن النظام مكتمل (إدخال + عرض)

| | الحالة |
|---|---|
| **عرض** ساعات العمل (في "عن المعرض") | ✅ كان موجوداً |
| **تحرير** ساعات العمل (في تحرير المعلومات) | ✅ أضفته الآن |

المعرض يُدخل ساعاته → تُحفظ → تظهر للزوّار في صفحة المعرض.

---

## ✅ آمن

- التعديل **إضافي** (قسم جديد + state + دوال)
- لم ألمس الحقول الموجودة (اسم، موقع، هاتف، bio)
- متوازن، Clock icon مستورد، WORK_DAYS معرّف

---

## الملف المعدّل (1)

`components/dealer-edit/info-edit-tab.tsx`

## النشر

```bash
git add components/dealer-edit/info-edit-tab.tsx
git commit -m "feat(dealer): working hours editor + keep whatsapp fix"
git push
```

## الاختبار
1. تحرير معلومات المعرض → اسحبي لأسفل → ✅ قسم "ساعات العمل" (7 أيام)
2. عدّلي أوقات يوم → احفظي → ✅ يُحفظ
3. اضغطي "إغلاق" ليوم → ✅ يصبح "مغلق"
4. افتحي صفحة المعرض → "عن المعرض" → ✅ ساعات العمل تظهر كما أدخلتِها
5. أدخلي رقم واتساب → احفظي → ✅ زر الواتساب يعمل (تصحيح المرحلة 2)

---

## 🔜 المراحل القادمة لصفحة المعارض

**المرحلة 4 — قص الصور قبل الرفع** (cropper) + رفع متعدد محسّن للمعرض.
**المرحلة 5 — حسابات التواصل الاجتماعي** (socialLinks).

> صفحة المعارض تقترب من الاكتمال. أغلب الأساسيات موجودة، نضيف اللمسات الأخيرة.

---

## ملاحظة صريحة

لم أشغّل build فعلياً (لا شبكة). تحققتُ يدوياً: متوازن ✅، `whatsapp`+`workingHours` في allowlist القاعدة ✅، Clock مستورد ✅، البنية تطابق UserProfile.workingHours ✅. المعاينة تؤكّد الواجهة. 

⚠️ هذا الـZIP (21) لم يكن محدّثاً بتصحيح المرحلة 2 — أبقيتُه في هذا الملف، فالرفع آمن. أرسلي أي خطأ.
