# 🏢 صفحة المعارض — المرحلة 1: زر واتساب

## التشخيص (مهم)

فحصتُ صفحة المعرض الحالية، والمفاجأة السارّة: **متطوّرة جداً بالفعل** وتحوي معظم متطلباتك:

| المتطلب | الحالة |
|---|---|
| Cover كبير + parallax | ✅ موجود |
| Logo دائري | ✅ موجود |
| اسم المعرض | ✅ موجود |
| التقييم | ✅ موجود |
| عدد الإعلانات | ✅ موجود |
| عدد المتابعين | ✅ موجود |
| زر متابعة | ✅ موجود |
| زر مراسلة | ✅ موجود |
| فلترة (سيارات/خدمات) | ✅ موجود (tabs) |
| badge موثق | ✅ موجود |
| **زر واتساب** | ❌ **كان ناقصاً** |
| عرض ساعات العمل | ❌ ناقص (الحقل موجود، لا يُعرض) |

## ما أصلحته (`trader-profile-header.tsx`)

### زر واتساب 🟢
أضفتُ زر واتساب أخضر لرأس المعرض:
- يظهر **فقط لو الرقم متاح** (`whatsapp` أو `phone`)
- يفتح `wa.me/{رقم}` مباشرة
- الصف يصبح **3 أعمدة** (متابعة + مراسلة + واتساب) لو واتساب متاح، أو **عمودين** تلقائياً لو لا

استخدم `normalizeLibyanPhone` (موجود) لتنسيق الرقم الليبي.

## ✅ آمن

- التعديل **إضافي** (زر جديد + تعديل grid)
- لم ألمس منطق المتابعة/المراسلة الموجود
- إضافتي متوازنة ذاتياً (4 أقواس مفتوحة = 4 مغلقة) — تحققتُ بمقارنة دقيقة

---

## الملف المعدّل (1)

`components/trader/trader-profile-header.tsx`

## النشر

```bash
git add components/trader/trader-profile-header.tsx
git commit -m "feat(dealer): add WhatsApp button to profile header"
git push
```

## الاختبار
1. افتحي صفحة معرض لديه رقم واتساب → ✅ 3 أزرار (متابعة/مراسلة/واتساب أخضر)
2. اضغطي واتساب → ✅ يفتح المحادثة
3. معرض بلا واتساب → ✅ زرّان فقط (تلقائياً)

---

## 🔜 المراحل القادمة لصفحة المعارض

صفحة المعرض الأساسية ممتازة. المتبقّي من طلبك (مرتّب):

**المرحلة 2 — عرض ساعات العمل**: الحقل `workingHours` موجود في UserProfile لكن لا يُعرض. أضيف قسماً يعرضها في تبويب "حول".

**المرحلة 3 — تحسين رفع الصور**: `compressImage` موجود للصور. إضافة:
- قص الصورة قبل الحفظ (cropper)
- رفع متعدد محسّن للمعرض (gallery)

**المرحلة 4 — حسابات التواصل الاجتماعي**: حقول socialLinks + عرضها.

**المرحلة 5 — قصص المعارض**: النظام موجود (dealer-stories). مراجعة وتحسين لو لزم.

> كل مرحلة منفصلة ومختبرة. أخبريني بأيّها نكمل.

---

## ملاحظة صريحة

لم أشغّل build فعلياً (لا شبكة). تحققتُ: إضافتي متوازنة ذاتياً، `normalizeLibyanPhone` و`phone`/`whatsapp` متاحة، المعاينة تؤكّد التصميم (3 أزرار/2 أزرار). 

**ملاحظة عن الـchecker**: الملف الأصلي يُظهر "()=-6" في أداة فحص التوازن لديّ (false positive من رموز في النص)، لكنه يعمل في الإنتاج. تعديلي حافظ على نفس التوازن تماماً (لم يضف خللاً). أرسلي أي خطأ build.
