# 🔧 إصلاح 3 نواقص في الربط

بعد الفحص العميق، وُجدت 3 ميزات مبنية لكن غير مربوطة. هذا الإصلاح يُفعّلها.

## الإصلاحات

### 1. إظهار المعارض الموثقة الجديدة في /traders ✅
**المشكلة**: صفحة قائمة المعارض كانت تفلتر بـ`isVerifiedDealer == true` فقط، فالمعارض التي وُثّقت عبر **شراء اشتراك** (`verifiedUntil`) لم تكن تظهر.

**الحل**: الآن تقرأ النظامين معاً (استعلامان متوازيان) وتدمجهما بإزالة التكرار + فلترة المنتهية.

**الملف**: `app/(public)/traders/page.tsx`

### 2. عرض معرض الصور في صفحة المعرض ✅
**المشكلة**: المستخدم يرفع صور المعرض (`dealerGallery`) من صفحة التعديل، لكن **لا أحد يراها** — لم يكن هناك مكان لعرضها.

**الحل**: أُضيف tab **"معرض الصور"** في صفحة المعرض:
- يظهر فقط لو للمعرض صور (يختفي تلقائياً لو فارغ)
- grid أنيق + lightbox عند الضغط (تكبير + عدّاد + إغلاق)

**الملف**: `components/trader/trader-tabs.tsx`

### 3. ربط تعديل المعرض من البروفايل ✅
**المشكلة**: `/profile/edit` (صفحة التحرير بالتابات) كانت تُفتح فقط من داخل صفحة المعرض، لا من البروفايل.

**الحل**: أُضيف زر **"تعديل المعرض"** بارز في البروفايل (للموثقين)، يفتح `/profile/edit` مباشرة.

**الملف**: `app/(public)/profile/page.tsx`

---

## خطوات النشر

### 1. ارفعي الملفات الثلاثة
```bash
git add "app/(public)/traders/page.tsx" \
        components/trader/trader-tabs.tsx \
        "app/(public)/profile/page.tsx"
git commit -m "fix: connect verified dealers, gallery tab, edit link"
git push
```

### 2. ✅ لا rules ولا dependencies جديدة

> ملاحظة: استعلام `verifiedUntil` يستخدم single-field index (يُنشأ تلقائياً في Firestore). أضفتُ `.catch()` احترازياً، فلو لم يكن جاهزاً، القائمة تعرض المعارض القديمة فقط دون أن تتعطّل.

---

## الاختبار

### إصلاح 1 (قائمة المعارض):
1. افتحي `/traders`
2. ✅ المعارض القديمة (isVerifiedDealer) + الجديدة (verifiedUntil) كلها تظهر

### إصلاح 2 (معرض الصور):
1. ارفعي صوراً من `/profile/edit` → tab "معرض الصور"
2. افتحي `/traders/{uid}` لنفس المعرض
3. ✅ يظهر tab "معرض الصور" بين "السيارات" و"عن المعرض"
4. اضغطي صورة → ✅ تكبر في lightbox

### إصلاح 3 (رابط التعديل):
1. ادخلي بحساب معرض موثق → `/profile`
2. ✅ سترين زر "تعديل المعرض" (تحت بطاقة "صفحة معرضي")
3. اضغطيه → ✅ يفتح `/profile/edit`

---

## ما تبقّى بعد هذا الإصلاح

| النقص | يحتاج |
|---|---|
| تقارير VIN حقيقية | عقود تجارية مع المزوّدين |
| MyFatoorah | حساب تاجر |
| تحويل الرصيد | رفع + rules (إن لم يُرفع بعد) |

كل النواقص المتبقية **خارجية** (تحتاج حسابات/عقود)، وليست مشاكل في الكود.
