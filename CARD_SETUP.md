# 🚗 بطاقة الإعلان الجديدة (Listing Card)

إعادة تصميم كاملة لبطاقة الإعلان بمستوى Dubizzle / OpenSooq، مع هوية Bratsho Car.

## الملف (1)
`components/listing-card.tsx` — استبدال كامل للبطاقة القديمة.

> **توافق كامل**: نفس الـprops (`listing`, `priority`) ونفس الـexports (named + default). كل الاستخدامات الـ7 (listings, favorites, my-listings, trader-tabs, listings-grid, featured-section) تعمل بلا تغيير.

---

## ما الجديد

### 1. الصورة
- ✅ نسبة 4:3 ثابتة
- ✅ Skeleton أثناء التحميل (`animate-pulse`)
- ✅ Lazy loading (priority للأوائل فقط)
- ✅ حواف دائرية + zoom ناعم عند hover

### 2. الشارات العلوية
- ✅ **يسار**: شارة واحدة بالأولوية: 🚀 ممول > ★ مميز > جديد
- ✅ **يمين**: زر الحفظ بتأثير Glassmorphism (`backdrop-blur`)

### 3. السعر
- ✅ Badge أزرق (brand-700) أسفل الصورة، حجم كبير واضح

### 4. عدّاد الصور
- ✅ شارة "📷 12" أسفل يسار الصورة (إن كان أكثر من صورة)

### 5. المواصفات
- ✅ سطر مدمج: 📅 سنة • ⛽ وقود • ⚙️ ناقل • 📍 مدينة (كلها تظهر فقط إن وُجدت)

### 6. بيانات البائع
- ✅ صورة (أو حرف أول) + اسم + ✓ توثيق + 🕐 وقت النشر

### 7. الإحصائيات
- ✅ 👁️ مشاهدات + ❤️ محفوظة (تظهر فقط إن كانت > 0)

### 8. الأزرار
- ✅ ثلاثة: واتساب (أخضر) + اتصال (أزرق) + مراسلة (أزرق فاتح)

### 9. الأداء
- ✅ `memo` بمقارنة مخصّصة (لا إعادة رسم إلا عند تغيّر مؤثّر)
- ✅ lazy images + skeleton
- ✅ Framer Motion دخول ناعم

### 10. الاستجابة + Dark Mode
- ✅ يعمل في كل الشبكات (2/3/4 أعمدة حسب الشاشة)
- ✅ دعم كامل للوضع الداكن

---

## التوافق مع الإعلانات القديمة (مضمون)

كل حقل جديد يُعرض **فقط إن وُجد**:
- لا صورة بائع → حرف أول من الاسم
- لا مشاهدات/محفوظة → السطر يختفي
- لا توثيق → بلا علامة ✓
- لا حالة سيارة → بلا شارة "جديد"

**لا حقل "ممول" في بياناتك حالياً** — الشارة جاهزة وتظهر تلقائياً لو أضفتِ `boostedUntil` مستقبلاً. حتى ذلك الحين، تظهر "مميز" (من `featured`) أو "جديد" (من `vehicleCondition`).

---

## النشر

```bash
git add components/listing-card.tsx
git commit -m "feat: redesign listing card (Dubizzle-level)"
git push
```

ملف واحد فقط. لا rules / dependencies / types جديدة.

> ملاحظة: زر "مراسلة" يربط `/messages?listing={id}`. إن كان مسار المحادثة مختلفاً في مشروعك، عدّلي السطر `href={\`/messages?listing=${listing.id}\`}`.

---

## المعاينة

افتحي `preview.html` في المتصفح لرؤية التصميم بدقة (صورة `preview.png` تقريبية — أداة التصيير لا تحمّل صور الإنترنت ولا Tailwind grid بدقة).

---

## الاختبار بعد النشر

1. افتحي `/listings`
2. ✅ بطاقات بتصميم احترافي: صورة 4:3، سعر بارز، شارات، بائع، إحصائيات، 3 أزرار
3. جرّبي إعلاناً قديماً → ✅ يظهر طبيعياً (بلا شارات للحقول المفقودة)
4. جرّبي الوضع الداكن → ✅ متناسق
5. اضغطي واتساب/اتصال/مراسلة → ✅ تعمل
