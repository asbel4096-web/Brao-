# 📄 إضافات صفحة تفاصيل الإعلان

إضافة الأقسام الناقصة + شريط تواصل ثابت — **دون إعادة بناء الصفحة** (حفاظاً على كل الوظائف).

## الملفات (3)

| الملف | النوع | الدور |
|---|---|---|
| `components/listing-sticky-cta.tsx` | معدّل | شريط ثابت + تتبّع النقرات |
| `components/similar-listings.tsx` | جديد | إعلانات مشابهة |
| `app/(public)/listings/[id]/page.tsx` | معدّل | ربط الجديد + رقم الإعلان + padding |

> **مهم**: هذا الملف يعتمد على `lib/track-event.ts` و `/api/listings/track` من جولة الإحصائيات السابقة (`brao-stats-tracking.zip`). ارفعي تلك أولاً.

---

## ما أُضيف

### 1. شريط تواصل ثابت (Sticky Bottom Bar) ✅
كان المكوّن `ListingStickyCta` موجوداً لكن **غير مستخدم** (يتيم). فعّلتُه + أضفتُ التتبّع:
- ✅ ثابت أسفل الشاشة (موبايل فقط)
- ✅ يختفي عند التمرير لأسفل، يظهر عند الصعود (مثل التطبيقات)
- ✅ Safe Area لـ iPhone (`env(safe-area-inset-bottom)`)
- ✅ Glassmorphism (`backdrop-blur` + خلفية بيضاء شفافة + ظل)
- ✅ واتساب الأبرز (أخضر، يأخذ المساحة) + اتصال + مراسلة
- ✅ Dark Mode
- ✅ Animation ناعم (translate-y)
- ✅ **يسجّل الإحصائيات** عند الضغط (واتساب/اتصال/مراسلة)
- ✅ نقرات المالك لا تُحتسب (الـAPI يستبعده)
- ✅ padding سفلي للصفحة (pb-32) كي لا يحجب المحتوى

### 2. إعلانات مشابهة ✅
مكوّن جديد `SimilarListings`:
- شريط أفقي من نفس الفئة (يُفضّل نفس المدينة)
- بطاقات مصغّرة (صورة + عنوان + سعر + مدينة)
- يختفي تماماً لو لا نتائج
- استعلام بسيط (بلا composite index)

### 3. رقم الإعلان ✅
يظهر أسفل الصفحة: `رقم الإعلان: #A1B2C3D4` (أول 8 أحرف من id).

### ما كان موجوداً بالفعل (لم نلمسه)
- ✅ معرض صور + fullscreen + عدّاد
- ✅ السعر/العنوان/شارة مميز
- ✅ المواصفات + الوصف (Read More)
- ✅ نصائح الأمان (`SafetyTipsCard`)
- ✅ بطاقة البائع + توثيق + زيارة الملف
- ✅ الموقع/الخريطة
- ✅ المشاركة (`ShareButton`)
- ✅ تتبّع المشاهدة (من جولة الإحصائيات)

---

## النشر

```bash
# أولاً: تأكدي من رفع brao-stats-tracking (track-event + API)
git add components/listing-sticky-cta.tsx \
        components/similar-listings.tsx \
        "app/(public)/listings/[id]/page.tsx"
git commit -m "feat: sticky CTA + similar listings + listing number"
git push
```

✅ لا rules / dependencies جديدة.
> إعلانات مشابهة: استعلام `where(category)` — single-field index (تلقائي). محمي بـ`.catch` فلن يكسر الصفحة.

---

## الاختبار

1. افتحي إعلاناً على الموبايل
2. ✅ شريط ثابت أسفل (واتساب/اتصال/مراسلة) — يبقى عند التمرير
3. مرّري لأسفل بسرعة → ✅ يختفي؛ اصعدي → يظهر
4. اضغطي أي زر → ✅ يُسجَّل في إحصائيات المالك
5. مرّري لأسفل الصفحة → ✅ "إعلانات مشابهة" + رقم الإعلان
6. على الديسكتوب → ✅ الشريط مخفي (الأزرار العادية كافية)

افتحي `preview.html` لرؤية البنية.
