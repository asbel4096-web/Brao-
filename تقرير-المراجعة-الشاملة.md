# تقرير المراجعة الشاملة — Bratsho Car (براتشو كار)

> مراجعة احترافية شاملة (Senior Architect / Full-Stack / Firebase / QA)
> منهجية: تحليل ثابت عميق على 291 ملفاً + إصلاح فعلي داخل المشروع.
> ⚠️ لم يُجرَ اختبار وقت تشغيل حيّ مع Firebase (يحتاج اختبارك اليدوي على النسخة المنشورة).

---

## 1) ما كان لا يعمل وتم إصلاحه

### 🔴 شجرة app/ مكرّرة داخل hooks/app/ (خطر كسر البناء)
نسخة ظلّية كاملة من مجلد `app/` (98 ملفاً، 1.6MB: صفحات + API + layout + globals.css + sitemap)
مدسوسة داخل `hooks/app/` من رفع GitHub خاطئ. بعضها نسخ قديمة مختلفة، وكلها تُفحَص أثناء
`next build` → خطر حقيقي لفشل البناء بأخطاء أنواع من ملفات قديمة. **حُذفت بالكامل.**

### 🔴 خطأ unchanged() الحرج في firestore.rules
النسخة المرفوعة رجعت للصيغة المعطوبة:
```
unchangedKeys().hasAll(keys)   // ترفض التعديل إذا غاب أي حقل محميّ
```
أُصلحت إلى deny-list آمنة:
```
!affectedKeys().hasAny(keys)   // تسمح ما لم يُمسّ حقل محميّ
```
هذا هو سبب "Missing or insufficient permissions" المتكرّر عبر الجلسات.

---

## 2) ما تم تحسينه
- إضافة حماية حقول الحملة/الإحصائيات لقائمة unchanged() في listings:
  boostPaused, boostPausedRemainingMs, boostNotif3d/1d/Expired,
  sponsoredImpressions, sponsoredClicks — يمنع تزوير الأرقام من العميل.
- التحقق من سلامة كامل الرسم البياني للاستيراد: 0 استيراد @/ مكسور من 291 ملف.
- فحص بناء شامل: لا أخطاء أقواس حقيقية (المتبقي إنذارات regex/JSX كاذبة).

---

## 3) ما تم تنظيفه (398 → 291 ملف)
- حذف hooks/app/ (98 ملف ميت).
- حذف 9 مكوّنات ميتة مؤكّدة (0 إشارة):
  category-showcase, listing-actions-bar, seller-card, push-foreground-listener,
  listing-quality-card, listing-sticky-cta, home-sections/latest-by-bucket,
  wallet/featured-badge, wallet/wallet-sheet.
- حذف hooks/useScrollDirection.ts (صار يتيماً بعد حذف listing-sticky-cta).
- حذف 43 ملف .md تقارير تطوير من الجذر (أُبقي README.md).

---

## 4) المشاكل المتبقية / إجراءات مطلوبة منك
1. ⚠️ إعادة نشر firestore.rules على Firebase Console (لا تُنشر عبر GitHub):
   Console → Firestore → Rules → الصق محتوى firestore.rules → Publish.
2. تحصين اختياري لـstorage.rules: قيّد كتابة brand-logos و homepage/banners
   بـ isAdmin() بدل أي مسجَّل (مُخفَّف حالياً لأن Firestore تمنع ربط الوثيقة).
3. اختبار وقت تشغيل يدوي لكل زرّ/نموذج/دردشة/رفع على النسخة المنشورة.

---

## 5) جاهزية الإطلاق: ~93%
البناء نظيف بنيوياً، القواعد سليمة، الكود الميت أُزيل.
المتبقي: إعادة نشر القواعد + اختبار حيّ يدوي + التحصين الاختياري.

---

## 6) قائمة الملفات المعدّلة/المحذوفة
معدّل:
- firestore.rules (إصلاح unchanged + إضافة حماية حقول الحملة)
محذوف:
- hooks/app/ (شجرة كاملة، 98 ملف)
- components/category-showcase.tsx
- components/listing-actions-bar.tsx
- components/seller-card.tsx
- components/push-foreground-listener.tsx
- components/listing-quality-card.tsx
- components/listing-sticky-cta.tsx
- components/home-sections/latest-by-bucket.tsx
- components/wallet/featured-badge.tsx
- components/wallet/wallet-sheet.tsx
- hooks/useScrollDirection.ts
- 43 × *.md (تقارير جذر)
