// ⚠️ أضيفي هذا إلى firestore.rules قبل القاعدة الأخيرة
//    match /{document=**} { allow read, write: if false; }
//
// إن كان لديكِ قاعدة لـ config مسبقاً، ادمجي فقط.

    // ============================================================
    // config/app - إعدادات التطبيق العامة (walletEnabled, ...)
    // ============================================================
    //   القراءة: مفتوحة للجميع (التطبيق يحتاج قراءة walletEnabled
    //            حتى للضيوف لإظهار/إخفاء زر المحفظة).
    //   الكتابة: ممنوعة من client - تتم عبر API route (Admin SDK) فقط.
    //            هذا يمنع أي مستخدم من تعطيل/تفعيل المحفظة بنفسه.
    match /config/{docId} {
      allow read: if true;
      allow write: if false;
    }
