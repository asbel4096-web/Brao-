# 🚗 إعادة تصميم صفحة الإعلان (مطابقة للمخطط 2026)

إعادة بناء كاملة لصفحة الإعلان بمستوى Dubizzle/OpenSooq/CarSwitch — **مع الحفاظ على كل المنطق.**

## ✅ الأقسام الثمانية (مطابقة للصورة المرجعية)

1. **معرض الصور** — صورة كبيرة Full Width، حواف 22px، Swipe + Zoom (عبر `ImageGallery`)، عدّاد الصور، شارات مميز/معتمد فوق الصورة، أزرار مشاركة/مفضلة
2. **شريط الصور المصغّرة** — داخل `ImageGallery` (موجود)
3. **بطاقة السعر** — السعر بارز أزرق `#2563EB` + اسم السيارة + المدينة + شارات موثّق/معتمد
4. **المواصفات السريعة** — 6 بطاقات (عداد/ناقل/وقود/سنة/ماركة/محرك) بأيقونات outline وخلفية فاتحة
5. **الوصف** — مختصر + "عرض المزيد" (توسعة بدون إعادة تحميل) + المميزات/العيوب
6. **معلومات التاجر** — صورة + اسم + توثيق + موقع + تقييم + متوسط الرد + زر "عرض المعرض"
7. **الإعلانات المشابهة** — Carousel أفقي (عبر `SimilarListings`)
8. **شريط التواصل الثابت** — واتساب أخضر `#22C55E` + اتصال أزرق `#2563EB`، ثابت أسفل + Safe Area

## ✨ تحسينات إضافية
- **Sponsored Badge ذهبي** `#F59E0B` فوق الصورة (للإعلانات الممولة: featured/vip/boosted)
- **Framer Motion**: Fade In للأقسام + scale عند الضغط على الأزرار
- وضع نهاري، خلفية `#F8FAFC`، بطاقات بيضاء، حواف 20-22px

## 🔒 المنطق المحفوظ (لم يُلمس)

✅ جلب الإعلان + البائع (`getDoc`) · ✅ **تتبّع المشاهدات** (`increment views`) · ✅ **بدء الدردشة** (`startChat` + `buildChatId` + إنشاء chat doc كامل) · ✅ `normalizeLibyanPhone` للواتساب · ✅ كل المكوّنات الموجودة (`ImageGallery`, `OwnerStatsBar`, `ListingComments`, `SafetyTipsCard`, `ReportButton`, `FavoriteButton`, `ShareButton`, `SimilarListings`) · ✅ معالجة loading/missing

**التغيير حصراً في الواجهة. منطق البيانات والدردشة نُسخ حرفياً.**

---

## الملفات (2)

| الملف | الحالة |
|---|---|
| `app/(public)/listings/[id]/page.tsx` | إعادة بناء كاملة |
| `components/similar-listings.tsx` | مُرفق (مطلوب للقسم 7 - لو لم يكن منشوراً) |

> لو `similar-listings.tsx` منشور مسبقاً لديك، تجاهلي نسخته هنا.

## النشر

```bash
git add "app/(public)/listings/[id]/page.tsx" components/similar-listings.tsx
git commit -m "redesign: modern 8-section listing detail page"
git push
```

⚠️ يتطلب `framer-motion` (مثبّت لديك).

## الاختبار

1. افتحي أي إعلان `/listings/[id]`
2. ✅ صورة كبيرة + شارات + عدّاد
3. ✅ السعر بارز أزرق
4. ✅ 6 بطاقات مواصفات
5. ✅ "عرض المزيد" يوسّع الوصف
6. ✅ بطاقة التاجر + "عرض المعرض"
7. ✅ إعلانات مشابهة (Carousel)
8. ✅ شريط ثابت أسفل (واتساب/اتصال) يبقى أثناء التمرير
9. ✅ زر الاتصال يفتح الهاتف، واتساب يفتح المحادثة، المراسلة تفتح الدردشة الداخلية

افتحي `preview.html` لرؤية البنية.

> ملاحظة: لم أشغّل build فعلي (لا شبكة). تحققتُ يدوياً: الأقواس متوازنة ✅ (فحص دقيق يتجاهل النصوص العربية)، كل دوال البيانات/الدردشة محفوظة، prop signatures للمكوّنات صحيحة (ImageGallery/OwnerStatsBar/SimilarListings). لو ظهر خطأ build، أرسلي screenshot.
