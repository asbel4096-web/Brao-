# 🔧 إصلاح نواقص لوحة الأدمن

5 نواقص وُجدت بالفحص العميق — تم إصلاحها كلها.

## الإصلاحات

### الروابط (#1, #2, #3) — ملف: admin-sidebar.tsx
- ✅ **#3 رابط مكسور**: أُزيل `/admin/settings/roles` (كان يؤدي لـ404 - لا صفحة له)
- ✅ **#1 صفحة يتيمة**: رُبط `/admin/wallet-settings` (إعدادات المحفظة)
- ✅ **#2 صفحة يتيمة**: رُبط `/admin/broadcast/history` (سجل البث)
- ✅ أُضيف رابطان للصفحتين الجديدتين أدناه

### #5 صفحة جديدة — مراقبة التحويلات
`app/admin/transfers/page.tsx`
- يقرأ `walletTransfers` realtime (آخر 200)
- إحصاءات: العدد، المجموع، اليوم
- بحث بالاسم/الهاتف
- للقراءة فقط (مراقبة + كشف عبث)

### #4 صفحة جديدة — إدارة قصص المعارض
`app/admin/dealer-stories/page.tsx`
- يقرأ `dealerStories` realtime (آخر 200)
- فلترة بالتصنيف (وصل حديثاً/عروض/المعرض/تجربة قيادة)
- grid مع صور + شارة المدة المتبقية + المشاهدات
- **حذف أي قصة مخالفة** (moderation)

---

## الملفات (3)

| الملف | النوع |
|---|---|
| `components/admin/layout/admin-sidebar.tsx` | معدّل (روابط) |
| `app/admin/transfers/page.tsx` | جديد |
| `app/admin/dealer-stories/page.tsx` | جديد |

---

## النشر

```bash
git add components/admin/layout/admin-sidebar.tsx \
        app/admin/transfers \
        app/admin/dealer-stories
git commit -m "fix(admin): link orphan pages + transfers & stories moderation"
git push
```

✅ لا rules جديدة (الـrules الحالية تسمح للأدمن بحذف dealerStories، وقراءة walletTransfers محمية بالـlayout).
✅ لا dependencies جديدة.

> ملاحظة: إن لم تكوني رفعتِ rules التحويلات/القصص بعد (من الجولات السابقة)، صفحة التحويلات قد تكون فارغة حتى تُرفع. صفحة القصص تعمل فور وجود قصص.

---

## الاختبار

1. ادخلي بحساب admin → لوحة التحكم
2. في الـsidebar (مجموعة "النظام") سترين روابط جديدة:
   - ✅ إعدادات المحفظة
   - ✅ تحويلات الرصيد
   - ✅ قصص المعارض
   - ✅ سجل البث (مجموعة الاتصال)
3. ✅ لا يوجد رابط "الأدوار والصلاحيات" المكسور بعد الآن
4. افتحي "تحويلات الرصيد" → ✅ تعرض التحويلات (إن وُجدت)
5. افتحي "قصص المعارض" → ✅ تعرض القصص + يمكن حذف أي واحدة

---

## ملخص حالة لوحة الأدمن بعد الإصلاح

| الجانب | الحالة |
|---|---|
| 26 صفحة أدمن | ✅ كلها تعمل |
| الحماية (isAdmin) | ✅ مركزية عبر layout |
| الروابط | ✅ كلها صحيحة (لا 404، لا يتيمة) |
| moderation القصص | ✅ جديد |
| مراقبة التحويلات | ✅ جديد |

لوحة الأدمن الآن **مكتملة ومترابطة بالكامل**.
