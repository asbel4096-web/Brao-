# المرحلة 6 — مراجعة نظام المحفظة والمدفوعات

## الخلاصة: النظام موجود وكامل بالفعل ✅
مشروعك فيه نظام محفظة ومدفوعات **production-ready** يغطّي كل ما طلبته في المواصفات.
لذلك ما بنيتش نظاماً جديداً (لأنه يكرّر ويكسر الموجود) — راجعته بالكامل وأضفت تحسيناً واحداً ناقصاً فقط.

## مطابقة المواصفات بالموجود

| المطلوب في المواصفات | الموجود فعلاً | الحالة |
|---|---|---|
| الترقية لا تُفعّل مباشرة، تمر عبر المحفظة | `app/api/wallet/boost/purchase/route.ts` يخصم الرصيد server-side قبل التفعيل | ✅ |
| collection `wallets` (balance) | الرصيد مخزّن في `users/{uid}.balance` | ✅ (مكافئ) |
| collection `transactions` | `walletTransactions` (credit/debit/boost/featured_listing...) | ✅ (أشمل) |
| collection `paymentRequests` | `topupRequests` (طلبات الشحن) | ✅ (نفس الفكرة) |
| صفحة المحفظة: رصيد + آخر العمليات + شحن + طلبات الشحن | `app/(public)/wallet/page.tsx` + `balance-hero` + `transactions-section` + `topup-sheet` | ✅ |
| ترقية → فحص رصيد → كافٍ: خصم+تفعيل / غير كافٍ: نافذة الشحن | كان ناقصاً جزء "نافذة الشحن" — **أضفته الآن** | ✅ (محسّن) |
| الأدمن: قبول/رفض طلبات الشحن | `app/admin/topup-requests` + `approve`/`reject` routes | ✅ |
| الأدمن: إضافة/خصم رصيد يدوي | `app/admin/wallet/[uid]` + `adjust` route | ✅ |
| Server-side validation | كل المسارات تتحقق على السيرفر (Admin SDK) | ✅ |
| Transaction protection | `runTransaction` في الشراء + الموافقة + التعديل | ✅ |
| Double-spending prevention | فحص `status !== "pending"` يمنع الموافقة المزدوجة، والخصم داخل transaction ذرّي | ✅ |
| Responsive (موبايل/تابلت/ديسكتوب) | كل الواجهات RTL ومتجاوبة | ✅ |

## التحسين الوحيد المُنفَّذ
**عند نقص الرصيد، تُفتح نافذة الشحن مباشرة** (كانت تكتفي بتنبيه فقط).

ملف واحد معدّل: `components/wallet/boost-sheet.tsx`
- زر "اشحن الرصيد" يظهر بدل "الرصيد غير كافٍ" ويفتح نافذة الشحن.
- زر "شحن" سريع بجانب الرصيد داخل نافذة الترقية.
- نافذة الشحن `TopupSheet` تُركّب فوق نافذة الترقية.

> هذا الملف **يشمل أيضاً** تعديلات المرحلة 5 (نوع "عاجل")، فهو النسخة الأحدث — استبدل به ملف المرحلة 5.

## ملاحظة مهمة (لا تكسر شيئاً)
يوجد مساران للأدمن يفعّلان "مميز" مجاناً بدون محفظة (منحة الأدمن `admin_grant` + نظام `featuredRequests` القديم). هذان **إجراءان إداريان** وليسا ترقية ذاتية من المستخدم، فما يخالفون قاعدة "ترقية المستخدم تمر عبر المحفظة". تركتهما كما هما حتى لا أُعطّل قدرة الإدارة على المنح المجانية. لو تبي إيقافهما، قول لي.

## لم أستطع تشغيل البناء هنا
البيئة بدون إنترنت و بدون `node_modules`. شغّل محلياً أو اعتمد على Vercel:
```
npm install
npm run type-check   # tsc --noEmit
npm run lint
npm run build
```
راجعت الأنواع والـ JSX يدوياً (توازن الأقواس ١٠٠٪).
