# تقرير: الصفحة الرئيسية (Hero Banner) + قصص صفحة المعارض

استبدل الملفات الأربعة كما هي عبر GitHub (نفس المسارات).

## 1) الملفات المعدّلة وسبب كل تعديل

### `app/page.tsx`
**السبب:** البانر كان يُرسم في **آخر** الصفحة. نُقل ليُعرض **مباشرة بعد القصص** كما هو مطلوب.
- الترتيب الجديد: `Header → Search → Stories → Hero Banner → Categories/Featured → Latest`.
- البانر يبقى متحكَّماً به من إعدادات الأدمن (يظهر فقط إن كان مفعّلاً)، لكن في موضعه الجديد أعلى الصفحة.
- نظّفت كوداً غير مستخدم (`EXTRA_KEYS`, `renderExtra`, import زائد) وأصلحت تكراراً في تعريف الدالة.

### `components/homepage-banners-carousel.tsx` (إعادة بناء كاملة)
**السبب:** كان فيه **خطأ في ترتيب React Hooks** (early return قبل `useEffect`) يسبّب أخطاء #310/#418، وتصميمه كان "صورة" لا Hero.
- ✅ إصلاح ترتيب الـHooks (كل الـHooks قبل أي return).
- ✅ Auto Play كل 5 ثوانٍ + يتوقف عند لمس/سحب المستخدم.
- ✅ Swipe يمين/يسار (scroll-snap يحترم RTL).
- ✅ Infinite Loop (بعد آخر شريحة يعود للأولى).
- ✅ Pagination Dots بيضاء فوق الصورة + أسهم على الديسكتوب.
- ✅ Smooth Transition + Skeleton أثناء التحميل + Lazy Loading للصور.
- ✅ تصميم Hero: ارتفاع مناسب للهاتف (170px → 250px للديسكتوب)، حواف `rounded-[26px]`، تدرّج احترافي، عنوان أكبر، وصف مختصر، وزر **CTA اختياري** ("اعرض التفاصيل") يظهر فقط لو للبنر رابط.

### `components/trader/trader-profile-header.tsx` (قصص صفحة المعرض)
**السبب:** زر "إضافة قصة" كان في **آخر** الصف (يظهر أسفل/بعد المحتوى)، والمالك يرى تصنيفات فارغة.
- ✅ نُقل زر "إضافة قصة" ليكون **أول بطاقة دائماً** (`[ + ] إضافة قصة` ثم القصص).
- ✅ إخفاء التصنيفات الفارغة → المالك بلا قصص يرى **زر الإضافة فقط** بلا فراغ.

### `components/stories/stories-row.tsx` (قصص الرئيسية)
**السبب:** عند عدم وجود قصص كان يظهر صندوق "لا توجد قصص" يترك مساحة.
- ✅ عند عدم وجود قصص: يبقى **زر الإضافة فقط** (أُزيل الصندوق الفارغ).

## 2) Safe Area (#5)
زر "إضافة قصة" **في أعلى** الصفحة (شريط القصص) — لا يقع خلف Bottom Navigation أبداً.
كما أن الـBottom Nav يحترم `env(safe-area-inset-bottom)` أصلاً (مُتحقَّق منه). لا حاجة لتغيير هنا.

## 3) Responsive (#7)
كل المكوّنات Mobile-First:
- البانر `w-full` داخل `container`، لا ينكسر على 320/375/390/430px.
- صفوف القصص `overflow-x-auto` + الزر `shrink-0` → ظاهر دائماً ولا يختفي.
- الأسهم على الديسكتوب فقط (`sm:grid`)، النقاط على الجميع.

## 4) قبل / بعد (ترتيب الصفحة)

```
        قبل                                  بعد
─────────────────────            ─────────────────────
Header                            Header
Search                           Search
Stories                          Stories
TowTrucks                        ★ Hero Banner (Slider)   ← نُقل هنا
BrowseByBrand                    TowTrucks
PlatformStats                    BrowseByBrand
Featured                         PlatformStats
...                              Featured
Latest                           ...
★ Banner (صورة بالأسفل)          Latest
CTA                              CTA
Footer                           Footer
```

البانر تحوّل من "صورة ثابتة بالأسفل" إلى **Hero Slider احترافي أعلى الصفحة** بأسلوب Dubizzle/OpenSooq.

## 5) ملاحظة عن البناء
البيئة هنا بدون إنترنت وبدون `node_modules`، فما قدرت أشغّل `npm run build`/TS/ESLint ولا ألتقط Screenshots حقيقية من التطبيق. راجعت يدوياً:
- توازن الأقواس في `page.tsx` (45/45) و البانر (64/64).
- إصلاح خطأ Hooks وتكرار الدالة.
شغّل للتأكد: `npm install && npm run build`. والمعاينة الحقيقية ستظهر على Vercel فوراً بعد الرفع.
