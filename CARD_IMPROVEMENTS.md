# 🎴 تحسين بطاقات الإعلانات

## ما تم

### 1. تبسيط بطاقة "أحدث الإعلانات" (تقليل الازدحام)
`listing-card.tsx` كانت تعرض في صف المعلومات: **السنة + الوقود + ناقل الحركة + المدينة** (4 عناصر = مزدحمة).

أصبحت تعرض: **السنة + المدينة** فقط.
- الوقود وناقل الحركة والمسافة → تبقى في صفحة الإعلان (تقليل الازدحام، قراءة أسرع).
- يبقى معروضاً: الصورة، السعر، العنوان، المدينة، السنة (كما طُلب بالضبط).

### 2. ✅ إصلاح تكرار "د.ل د.ل"
في الصورة التي أرسلتِها ظهر السعر **"24.800 د.ل د.ل"** (مكرّر).

**السبب**: `formatPrice()` يضيف "د.ل" تلقائياً، ثم البطاقة كانت تضيف `<span>د.ل</span>` مرة أخرى.

**الإصلاح**: إزالة الـ"د.ل" اليدوية من البطاقتين (`listing-card` + `compact-listing-card`). الآن تظهر مرة واحدة فقط: **"24.800 د.ل"**.

> فحصتُ كل المكوّنات: فقط هاتان البطاقتان كانتا مكرّرتين. `my-listing-card` سليم (يستخدم formatPrice فقط).

---

## الملفات المعدّلة (2)

| الملف | التغيير |
|---|---|
| `components/listing-card.tsx` | تبسيط صف المعلومات (سنة+مدينة) + إصلاح تكرار د.ل + إزالة أيقونات غير مستخدمة (Fuel/Settings2) |
| `components/compact-listing-card.tsx` | إصلاح تكرار د.ل |

## النشر

```bash
git add components/listing-card.tsx components/compact-listing-card.tsx
git commit -m "improve: simplify listing card + fix duplicate currency"
git push
```

## الاختبار
1. الرئيسية → أحدث الإعلانات → ✅ بطاقة أنظف (سنة + مدينة فقط)
2. كل البطاقات → ✅ السعر يظهر "د.ل" مرة واحدة فقط
3. الأقسام الأفقية (مميزة/أكثر مشاهدة) → ✅ لا تكرار د.ل

> لم أشغّل build فعلي (لا شبكة). تحققتُ يدوياً: الأقواس متوازنة ✅، الأيقونات غير المستخدمة أُزيلت، الحقول المطلوبة (عنوان/سعر/مدينة/سنة) محفوظة، باقي منطق البطاقة (البائع/الوقت/الشارات/المفضلة) لم يُلمس.
