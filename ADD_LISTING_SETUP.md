# 🚗 إعادة تصميم صفحة "أضف سيارة جديدة"

إعادة بناء كاملة للواجهة بمستوى Dubizzle/OpenSooq — **مع الحفاظ على كل منطق الحفظ الموجود.**

## ✅ ما تم

### التصميم (مطابق للمرجع)
- وضع نهاري، خلفية `#F8FAFC`، بطاقات بيضاء بظلال خفيفة، زوايا 16-24px
- اللون الأساسي الأزرق، RTL عربي احترافي، Mobile First
- Framer Motion للانتقالات بين الخطوات + الـStepper

### Stepper احترافي (5 خطوات)
1. الصور · 2. المعلومات · 3. التفاصيل · 4. السعر · 5. نشر الإعلان
- الحالية أزرق، المكتملة ✓، خط تقدّم متحرك، قابلة للنقر للرجوع

### قسم الصور
- منطقة رفع كبيرة + **Drag & Drop**
- الصورة الرئيسية كبيرة (4:3) + مصغّرات أسفلها
- حذف بأيقونة X · اضغط مصغّرة لجعلها رئيسية
- الحد الأدنى 3 صور · الأقصى 20

### المعلومات (Grid حديث)
الماركة/الموديل/السنة/المسافة/الوقود/الناقل — حقول نظيفة داخل بطاقة.

### المواصفات (Chips)
12 ميزة كـchips: مكيف، كاميرا، حساسات، بلوتوث، فتحة سقف، مثبت سرعة، بصمة، شاشة، ملاحة، جنوط، جلد، تحكم مقود.
- المختارة: خلفية زرقاء + علامة ✓

### السعر
حقل كبير وسط بطاقة + عرض العملة + معاينة فورية + أزرار سريعة.

### الموقع
اختيار المدينة + العنوان + رابط الخريطة (اختياري).

### Sticky Bottom Bar
ثابت أسفل + Blur + ظل + **Safe Area** لـiPhone + زرّا السابق/التالي (ونشر في النهاية).

---

## 🔒 ما حُوفظ عليه (لم يُلمس منطقه)

✅ **كل حقول FormState الـ27** · ✅ رفع الصور لـFirebase Storage + **العلامة المائية** · ✅ `addDoc` بكل الحقول · ✅ فحص الكلمات المحظورة · ✅ حقول الساحبات الشرطية (`showTowTruckFields`) · ✅ `entityType` · ✅ التحقق لكل خطوة · ✅ معالجة الأخطاء · ✅ Pre-fill من الملف الشخصي · ✅ الحارس (تسجيل الدخول).

**التغيير حصراً في الواجهة (JSX + التصميم). منطق الحفظ والتحقق نُسخ حرفياً.**

> تغيير وحيد منطقي مقصود: التحقق وُزّع على 5 خطوات بدل 4 (الصور صارت خطوة منفصلة، السعر خطوة منفصلة) — مطابقةً للمرجع.

---

## الملف المعدّل (1)

`app/(public)/add-listing/page.tsx` (إعادة بناء كاملة)

## النشر

```bash
git add "app/(public)/add-listing/page.tsx"
git commit -m "redesign: modern 5-step add-listing UI (logic preserved)"
git push
```

⚠️ يتطلب `framer-motion` (مثبّت لديك بالفعل).

## الاختبار

1. افتحي `/add-listing`
2. ✅ Stepper بـ5 خطوات أعلى
3. أضيفي 3+ صور (جرّبي Drag&Drop على ديسكتوب) → ✅ رئيسية كبيرة + مصغّرات
4. أكملي المعلومات + اختاري chips → ✅ تتحوّل زرقاء
5. السعر → ✅ معاينة فورية
6. التواصل → نشر → ✅ يُحفظ في Firestore (نفس المنطق القديم)
7. تأكدي أن الإعلان ظهر في "إعلاناتي" بعد النشر

افتحي `preview.html` لرؤية البنية.

> **ملاحظة**: لم أتمكّن من تشغيل build فعلي (لا شبكة) — تحققتُ يدويًا من توازن الأقواس (✅)، وأن كل دوال الحفظ والحقول الـ27 محفوظة، وأن الـimports صحيحة. لو ظهر خطأ build، أرسلي screenshot.
