# 🏢 صفحة المعارض — المرحلة 2: إصلاح حقل الواتساب + ساعات العمل

اكتشفتُ خطأً حقيقياً مهماً أثناء هذه المرحلة.

## 🔴 خطأ مكتشَف ومُصلَح: حقل الواتساب الخاطئ

### المشكلة
- الحقل الرسمي في UserProfile: **`whatsapp`**
- لكن `info-edit-tab` (تحرير معلومات المعرض) كان يحفظ في **`whatsappNumber`** (حقل خاطئ!)
- وكل شيء يقرأ من `whatsapp` (زر الواتساب الذي أضفته، صفحة المعرض...)

### النتيجة (خطأان في آن واحد)
1. **رقم الواتساب لا يظهر أبداً**: المعرض يُدخله، يُحفظ في `whatsappNumber`، لكن يُقرأ من `whatsapp` → فارغ دائماً.
2. **التحديث كان يُرفض (permission-denied)**: قاعدة الأمان تسمح فقط بحقول محدّدة (`hasOnly` allowlist)، و`whatsappNumber` **ليس** فيها — فمحاولة حفظه تفشل بصمت!

### الإصلاح (`info-edit-tab.tsx`)
```js
// قبل: whatsappNumber: whatsappNumber.trim()  ❌ (حقل خاطئ + يُرفض)
// بعد: whatsapp: whatsappNumber.trim()         ✅ (الحقل الصحيح + مسموح)
```
+ القراءة الأولية تقرأ `whatsapp` (مع fallback لـ`whatsappNumber` القديم للبيانات الموجودة).

> تأكدتُ أن `whatsapp` موجود في allowlist القاعدة (`hasOnly`) → التحديث سينجح الآن.

---

## ✅ ساعات العمل: مُنفّذة سلفاً (عرضاً)

فحصتُ المشروع: **عرض ساعات العمل مُنفّذ بالكامل بالفعل** — يوجد `WorkingHoursList` في تبويب "عن المعرض" يعرض كل الأيام (السبت-الجمعة) مع الأوقات و"مغلق". `workingHours` موجود في القاعدة + allowlist.

### الفجوة الحقيقية: لا واجهة لـ**تحرير** ساعات العمل
`info-edit-tab` يحرّر فقط (اسم، موقع، هاتف، واتساب، bio) — **لا حقول لإدخال ساعات العمل**. لذا تظهر دائماً "لم يتم تحديد ساعات العمل".

> بناء واجهة تحرير ساعات العمل (7 أيام × open/close/مغلق) = مكوّن كامل. أتركه للمرحلة 3 لو أردتِه — مكوّن منفصل ومختبر. أخبريني.

---

## الملف المعدّل (1)

`components/dealer-edit/info-edit-tab.tsx`

## النشر

```bash
git add components/dealer-edit/info-edit-tab.tsx
git commit -m "fix(dealer): save whatsapp to correct field (was whatsappNumber)"
git push
```

## الاختبار (مهم)
1. افتحي تحرير معلومات المعرض → أدخلي رقم واتساب → احفظي
2. ✅ يُحفظ بنجاح (كان يفشل بصمت قبل الإصلاح)
3. افتحي صفحة المعرض → ✅ زر الواتساب يظهر ويعمل
4. ساعات العمل (لو مُدخلة في القاعدة) → ✅ تظهر في "عن المعرض"

---

## 🔜 المراحل القادمة

**المرحلة 3 — واجهة تحرير ساعات العمل**: مكوّن لإدخال 7 أيام (open/close/مغلق) في info-edit-tab.

**المرحلة 4 — قص الصور قبل الرفع** (cropper) + رفع متعدد محسّن.

**المرحلة 5 — حسابات التواصل الاجتماعي**.

---

## ملاحظة صريحة

لم أشغّل build فعلياً (لا شبكة). تحققتُ يدوياً: متوازن ✅، `whatsapp` في allowlist القاعدة ✅، `whatsappNumber` مستخدم فقط في هذا الملف (لا أكسر شيئاً) ✅. هذا الإصلاح يحلّ خطأً حقيقياً كان يمنع حفظ/ظهور الواتساب. أرسلي أي خطأ.
