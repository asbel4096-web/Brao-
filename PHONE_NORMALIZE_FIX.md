# 📱 إصلاح: "أرقام تعمل وأخرى لا" — الصفر البادئ

## 🎯 السبب الجذري (خطأ كود مؤكّد)

في `LoginClient.tsx`، كان الرقم يُبنى هكذا:
```js
const fullPhone = `+218${phoneDigits}`;  // ❌ بلا إزالة الصفر
```

والحقل يقبل **حتى 10 أرقام**. أرقام ليبيا المحلية تبدأ بـ`0`:

| ما يُدخله المستخدم | fullPhone الناتج | النتيجة |
|---|---|---|
| `913441664` (بلا صفر) | `+218913441664` ✅ | **يعمل** |
| `0913441664` (مع صفر) | `+2180913441664` ❌ | **يفشل** (رقم خاطئ، لا يصل SMS) |

**هذا بالضبط سبب "أرقام تعمل وأخرى لا"**: الفرق هو إدخال الصفر البادئ من عدمه. من يكتب رقمه بالعادة الليبية (مع 0) يفشل، ومن يكتبه بلا 0 ينجح.

## ✅ الإصلاح

أُزيل الصفر (أو الأصفار) البادئة تلقائياً قبل بناء الرقم الدولي:
```js
const nationalDigits = phoneDigits.replace(/\D/g, "").replace(/^0+/, "");
const fullPhone = `+218${nationalDigits}`;
```

الآن الرقم يعمل **سواء أدخله المستخدم بصفر أو بدونه**:
- `0913441664` → يُزال الصفر → `+218913441664` ✅
- `913441664` → `+218913441664` ✅

كما حُدّث التحقق المحلي ليعتمد على الرقم بعد التطبيع.

## الملف المعدّل (1)

`app/(public)/login/LoginClient.tsx`

## النشر

```bash
git add "app/(public)/login/LoginClient.tsx"
git commit -m "fix: strip leading zero from Libyan phone before E.164"
git push
```

## الاختبار

بعد النشر، جرّبي تسجيل الدخول بـ:
1. رقم **مع صفر**: `0913441664` → ✅ يصل الرمز الآن (كان يفشل)
2. رقم **بلا صفر**: `913441664` → ✅ يعمل كما قبل

كلاهما يصبح `+218913441664` داخلياً.

## ملاحظة

التحذيران المتبقيان في console (`apple-mobile-web-app-capable deprecated` و `reCAPTCHA Enterprise → v2`) **غير ضارّين** — الأول تحذير تجميلي، والثاني سلوك طبيعي (Firebase يستخدم reCAPTCHA v2 بنجاح). لا يؤثران على تسجيل الدخول.
