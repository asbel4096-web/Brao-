# 🏢 صفحة المعارض — المرحلة 5: حسابات التواصل الاجتماعي

المرحلة الأخيرة لصفحة المعارض. + فحص معرض الصور والغلاف.

## فحص معرض الصور والغلاف (كما طلبتِ)

| | الحالة |
|---|---|
| **معرض الصور** (gallery-edit-tab) | ✅ سليم ومتطوّر: رفع متعدد، مؤشّر تقدّم، حد أقصى، تحقّق حجم، ضغط |
| **الغلاف** (image-edit-tab cover) | ✅ سليم: يستخدم uploadDealerCover (مع قص 16:9 من المرحلة 4) |

**لا مشاكل تكسرهما.** ملاحظة بسيطة: رفع المعرض تسلسلي (صورة تلو الأخرى) — مناسب للمؤشّر التدريجي، الفرق في السرعة طفيف.

---

## المرحلة 5: التواصل الاجتماعي (3 أجزاء)

`facebookUrl`/`instagramUrl` كانا موجودين في UserProfile لكن: غير مسموحين في القاعدة + لا إدخال + لا عرض. أكملتُ الثلاثة:

### 1. القاعدة (`firestore.rules`)
أضفتُ `facebookUrl` + `instagramUrl` لـallowlist (`hasOnly`). **بدونهما الحفظ يُرفض** (permission-denied). يجب نشر القاعدة:
```bash
firebase deploy --only firestore:rules
```

### 2. واجهة الإدخال (`info-edit-tab.tsx`)
حقلا إدخال (فيسبوك + إنستغرام) بأيقوناتهما، تُحفظ في الحقلين الصحيحين.

### 3. العرض (`trader-tabs.tsx`)
في تبويب "عن المعرض" → معلومات التواصل، يظهر رابطا فيسبوك/إنستغرام (لو متوفّران) بأيقونتيهما.

---

## ✅ آمن

- التعديلات **إضافية** (حقول + عرض جديد)
- لم ألمس المنطق الموجود (اسم، هاتف، واتساب، ساعات العمل)
- الملفات الثلاثة متوازنة

---

## الملفات المعدّلة (3)

| الملف | التغيير |
|---|---|
| `firestore.rules` | + facebookUrl/instagramUrl في allowlist |
| `components/dealer-edit/info-edit-tab.tsx` | + حقلا إدخال |
| `components/trader/trader-tabs.tsx` | + عرض الروابط |

## النشر

```bash
git add firestore.rules components/dealer-edit/info-edit-tab.tsx components/trader/trader-tabs.tsx
git commit -m "feat(dealer): social media links (facebook + instagram)"
git push
firebase deploy --only firestore:rules   # مهم - بدونه الحفظ يُرفض
```

## الاختبار
1. تحرير المعرض → ✅ حقلا فيسبوك/إنستغرام
2. أدخلي رابطاً → احفظي → ✅ يُحفظ (بعد نشر القاعدة)
3. صفحة المعرض → "عن المعرض" → ✅ الرابطان يظهران

---

## 🎉 صفحة المعارض — مكتملة

| الميزة | الحالة |
|---|---|
| Cover + Logo + اسم + تقييم + متابعين | ✅ |
| زر اتصال + واتساب + متابعة + مراسلة | ✅ |
| ساعات العمل (إدخال + عرض) | ✅ |
| قص الصور (logo 1:1، cover 16:9) | ✅ |
| معرض صور (رفع متعدد + ضغط) | ✅ |
| التواصل الاجتماعي | ✅ |
| فلترة الإعلانات (tabs) | ✅ |

صفحة المعارض الآن احترافية وكاملة.

---

## ⚠️ بخصوص "المشروع كامل سريع وسلس يدعم كل الأجهزة"

هذا **طلب كبير منفصل** — ليس تسليماً واحداً. يحتاج:
- تدقيق أداء (Core Web Vitals، أحجام bundles، lazy loading)
- تدقيق Responsive على كل المقاسات (320px جوال صغير → 1280px تابلت/ماك)
- اختبار فعلي على أجهزة مختلفة

**اقتراحي**: أخصّص له **جلسة تدقيق منفصلة** بعد رفع المراحل الحالية. سأفحص:
- نقاط الأداء الفعلية (ما يبطئ التحميل)
- نقاط الكسر في التصميم على المقاسات المختلفة
- وأقدّم إصلاحات محدّدة

لا أستطيع ضمان "يعمل على كل الأجهزة بلا أخطاء" دون اختبار فعلي على تلك الأجهزة (بيئتي لا تملكها). لكن يمكنني تدقيق الكود لاكتشاف المشاكل الشائعة وإصلاحها. أخبريني لو تريدين هذه الجلسة.

---

## ملاحظة صريحة

لم أشغّل build فعلياً (لا شبكة). تحققتُ يدوياً: الملفات الثلاثة متوازنة ✅، الحقول في allowlist ✅، الأيقونات مستوردة (Facebook/Instagram من lucide-react) ✅. بنيتُ على نسختك المحدّثة (Brao--main__22). أرسلي أي خطأ.
