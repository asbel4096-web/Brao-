# 📋 مراجعة نظام الصور — التقرير النهائي

## مراجعة شاملة (قبل التنفيذ)

راجعتُ كل ملفات رفع/حذف الصور. النتيجة الدقيقة لكل نوع:

| النوع | المسار | الاستبدال | الحذف | الحالة |
|---|---|---|---|---|
| **Logo المعرض** | ثابت `logo.jpg` | يكتب فوق القديم | — | ✅ آمن (لا يتيمة) |
| **Cover المعرض** | ثابت `cover.jpg` | يكتب فوق القديم | — | ✅ آمن (لا يتيمة) |
| **Gallery المعرض** | فريد | — | `deleteImageByURL` عند الحذف | ✅ آمن |
| **حذف البانر** | فريد | — | `deleteObject` في deleteBanner | ✅ آمن |
| **استبدال صورة البانر** | فريد `_edit_` | يرفع جديدة | ❌ **كان يترك القديمة يتيمة** | 🔧 **أصلحته** |
| **غلاف البروفايل** | فريد `cover-` | يرفع جديدة | ❌ **كان يترك القديم يتيماً** | 🔧 **أصلحته** |
| **صور القصص المنتهية** | فريد | — | ❌ لا تُحذف بعد 24h | ⚠️ متبقٍّ (cron) |

---

## ما نفّذته

### إصلاح 1: حذف الصورة القديمة عند استبدال صورة البانر (`app/admin/content/homepage/page.tsx`)

عند تعديل بانر ورفع صورة جديدة:
1. أحفظ رابط الصورة القديمة قبل الرفع
2. أرفع الجديدة + أحدّث Firestore
3. **بعد نجاح التحديث فقط** → أحذف القديمة من Storage

### إصلاح 2: حذف الغلاف القديم عند تغيير غلاف البروفايل (`app/(public)/profile/page.tsx`)

نفس النمط: عند رفع غلاف جديد، أحذف القديم **بعد** نجاح تحديث `coverURL`.

**حماية من حذف الجديدة بالخطأ** (في الإصلاحين):
- الحذف **بعد** نجاح التحديث (لو فشل، القديمة تبقى)
- شرط `old !== new` (لا يحذف لو نفس الرابط)
- شرط الرابط الجديد موجود (لا يحذف لو لم تُرفع صورة)
- `try/catch` (فشل الحذف لا يكسر العملية)

---

## ✅ التأكيدات المطلوبة

### Memory Leak
- ✅ لا تسريب: `URL.createObjectURL` للمعاينات يُنشأ عند الاختيار. (ملاحظة: المعاينات المؤقتة تُحرّر تلقائياً عند إغلاق الصفحة - لا تسريب دائم)
- ✅ لا مستمعات أحداث معلّقة في تعديلي

### ملفات يتيمة في Storage
- ✅ logo/cover: مسار ثابت (يكتب فوق) - لا يتيمة
- ✅ gallery: يُحذف عند الحذف
- ✅ banner delete + **banner replace** (أصلحته الآن): يُحذف
- ⚠️ **صور القصص المنتهية**: تبقى (انظر المتبقّي)

---

## الملفات المعدّلة (2)

| الملف | التغيير |
|---|---|
| `app/admin/content/homepage/page.tsx` | حذف صورة البانر القديمة عند الاستبدال |
| `app/(public)/profile/page.tsx` | حذف غلاف البروفايل القديم عند الاستبدال |

### جديدة (0)

---

## النشر

```bash
git add "app/admin/content/homepage/page.tsx" "app/(public)/profile/page.tsx"
git commit -m "fix(storage): delete old image on replace (banner + profile cover)"
git push
```

## الاختبار
1. عدّلي بانراً → ارفعي صورة جديدة → احفظي → ✅ القديمة تُحذف من Storage
2. عدّلي بانراً بدون تغيير الصورة → ✅ الصورة تبقى (لا حذف)
3. لو فشل التحديث → ✅ القديمة تبقى (لا حذف قبل النجاح)

---

## ⚠️ بصراحة: البند 2 (صورة الجوال للإعلان)

طلبتِ فصل "صورة الجوال" عن "الصورة الرئيسية" في **تعديل الإعلان**. لكن بعد المراجعة:

**الإعلانات لا تملك صورة جوال منفصلة.** الإعلان يستخدم `images[]` (مصفوفة صور عادية). صفحة تعديل الإعلان (`my-listings/[id]/edit`) **لا تعدّل الصور إطلاقاً** (نصوص فقط: عنوان/سعر/وصف).

مفهوم "صورة رئيسية + صورة جوال منفصلة" ينطبق على **البانر** (له `imageUrl` + `mobileImageUrl`) - وهذا موجود بالفعل في لوحة تحكم البانرات.

**لذا البند 2 كما وُصف لا ينطبق على الإعلانات.** الخيارات:
- (أ) لو تقصدين البانر: الفصل موجود (imageUrl رئيسية + mobileImageUrl للجوال في نموذج الإضافة)
- (ب) لو تريدين فعلاً صورة جوال منفصلة **للإعلانات**: هذا مفهوم جديد يتطلب حقل `mobileImage` في الإعلان + تعديل بنية الإعلانات + صفحة التعديل (عمل كبير). أخبريني لو هذا مقصودك.

لم أنفّذ (ب) لأنه يغيّر بنية الإعلانات جذرياً ولم أتأكد أنه مقصودك. أوضّحي وأنفّذ.

---

## نتيجة Build / TypeScript — بصراحة

⚠️ **لا أستطيع تشغيلها** (بيئتي بلا شبكة/تبعيات). **لن أدّعي نجاحاً.**

تحققتُ يدوياً:
- ✅ الملف متوازن
- ✅ deleteObject مستورد
- ✅ الحذف بعد نجاح التحديث + شروط الحماية (≠ القديمة، newImageUrl موجود)
- ✅ try/catch يمنع كسر العملية

**شغّلي `npm run build` محلياً وأرسلي أي خطأ.**

---

## المشاكل المتبقية (صراحة)

1. **صور القصص المنتهية (24h) تبقى في Storage**: لا cron ينظّفها. حذفها يحتاج cron بـAdmin SDK (server-side) يمرّ على القصص المنتهية ويحذف صورها. عمل منفصل - أخبريني لو أردتِه.

2. **البند 2 (صورة جوال للإعلان)**: يحتاج توضيحاً (انظر أعلاه) - الإعلانات لا تملك هذا المفهوم حالياً.

3. **اختبار فعلي** يجب أن تجريه أنتِ.
