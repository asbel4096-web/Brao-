# 🏠 إعادة تنظيم الصفحة الرئيسية + إحصائيات حقيقية

## ✅ ما نُفّذ

### 1. البنر الرئيسي (سلايدر) — أعلى الصفحة
البنر (`HomepageBannersCarousel`) كان في الأسفل ضمن أقسام الأدمن. نُقل **أعلى الصفحة مباشرة بعد البحث**. يظهر فقط لو وُجدت بنرات (`banners.length > 0`). يدعم عدّة بنرات + النقر للرابط (الميزات موجودة في المكوّن).

### 2. قسم "تعطّلت سيارتك؟" — كما هو
لم يُلمس منطقه (`TowTrucksCTA`)، فقط أُعيد ترتيبه.

### 3. تصفّح حسب الماركة — كما هو
`BrowseByBrand` بلا تغيير، أُعيد ترتيبه أسفل "تعطّلت سيارتك؟".

### 4. ✅ إصلاح الإعلانات المميزة (مهم)
**المشكلة الجذرية**: `isListingFeatured` كان يتطلب `featured===true` **و** `featuredUntil` في المستقبل. أي إعلان مميّز **بلا تاريخ انتهاء** كان **يُخفى** → سبب عدم ظهور المميزة!

**الإصلاح** (`lib/utils.ts`):
- يدعم الآن **كلا الحقلين**: `featured` **و** `isFeatured`
- المميّز بلا تاريخ انتهاء → يُعتبر مميّزاً (لا يُخفى)
- بيانات تاريخ ناقصة → لا تُسقط الإعلان

**`featured-near-you.tsx`**: يستعلم الآن **الحقلين معاً** (استعلامان متوازيان + دمج بلا تكرار)، لأن Firestore لا يدعم OR على حقلين في استعلام واحد. القسم يختفي تلقائياً لو لا توجد مميزة (`visible.length === 0 → return null`).

### 5. ✅ الإحصائيات الحقيقية (إلغاء الأرقام الوهمية)
أنشأتُ `hooks/usePlatformStats.ts` — أرقام حقيقية من Firestore عبر `getCountFromServer` (عدّ كفؤ، لا يقرأ كل المستندات):
- 🚗 **السيارات**: `count(category == "سيارات")`
- 🔧 **قطع الغيار**: `count(category in [5 فئات قطع غيار])`
- 🏢 **المعارض**: `count(verificationType == "dealer") + count("showroom")`
- 👤 **المستخدمون النشطون**: `count(lastLoginAt >= آخر 30 يوم)`

**`platform-stats.tsx`** أُعيد بناؤه: Skeleton أثناء التحميل، معالجة أخطاء (أصفار بدل الانهيار)، كاش 10 دقائق لتقليل القراءات.

> كل الاستعلامات single-field → **لا فهارس مركّبة مطلوبة**، تعمل فوراً.

### 6. الترتيب النهائي
```
بحث → البنر → قصص → تعطّلت سيارتك → ماركة → المميزة →
أحدث الإعلانات → الأكثر حفظاً → المعارض → الإحصائيات → فوتر
```

### 7. تحسينات
- mounted gate محفوظ (لا أخطاء hydration)
- القصص (StoriesRow) دون تعديل كما طلبتِ
- لا حذف لأي وظيفة

---

## الملفات (4)

| الملف | التغيير |
|---|---|
| `app/page.tsx` | إعادة ترتيب + البنر للأعلى |
| `hooks/usePlatformStats.ts` | **جديد** — عدّ حقيقي من Firestore |
| `components/platform-stats.tsx` | أرقام حقيقية + Skeleton |
| `components/featured-near-you.tsx` | دعم featured + isFeatured |
| `lib/utils.ts` | isListingFeatured يدعم isFeatured + بلا تاريخ |

## النشر

```bash
git add app/page.tsx hooks/usePlatformStats.ts components/platform-stats.tsx \
        components/featured-near-you.tsx lib/utils.ts
git commit -m "feat: real stats + featured fix + homepage reorder"
git push
```

## الاختبار
1. الرئيسية → ✅ البنر أعلى (لو وُجدت بنرات)
2. الترتيب الجديد مطبّق
3. الإعلانات المميزة → ✅ تظهر الآن (حتى بلا featuredUntil)
4. الإحصائيات → ✅ أرقام حقيقية + Skeleton أثناء التحميل
5. console → ✅ لا أخطاء

---

## ⚠️ نقطة لم تُنفّذ (تحتاج جولة): "أحدث قطع غيار" كقسم منفصل

`ListingsGrid` الحالي لا يقبل فلتر `category`. لإضافة قسم "أحدث قطع غيار" منفصل (كما في المخطط)، يحتاج `ListingsGrid` إمّا prop جديد `category` أو مكوّن جديد. أخبريني وأنفّذه في جولة قادمة معزولة — لم أردْ تعديل `ListingsGrid` (يُستخدم في أماكن أخرى) دون اختبار.

> ملاحظة: لم أشغّل build فعلي (لا شبكة). تحققتُ يدوياً: كل الملفات متوازنة ✅، لا متغيّرات غير مستخدمة، الاستعلامات single-field (لا فهارس)، الـimports صحيحة.
