# 📋 نافذة تعديل البانر — تعديل الصورة + مسح العنوان

أكملتُ البندين.

## 1. تعديل الصورة في نافذة التعديل

### ما أضفته (`app/admin/content/homepage/page.tsx`)
- **حقل رفع صورة** أعلى نافذة التعديل، يعرض الصورة الحالية كمعاينة
- `handleEditFile`: تحقّق حجم (3MB) + معاينة
- `openEdit`: يضع الصورة الحالية (`b.imageUrl`) كمعاينة افتراضية
- `handleSaveEdit`: لو اختير ملف جديد، يُرفع لـStorage ويُحدّث `imageUrl`؛ وإلا تبقى الصورة الحالية

النتيجة: تعديل صورة بانر مباشرة من النافذة (بدل حذف وإعادة إضافة).

## 2. مسح العنوان للفراغ

### المشكلة
`updateBanner` يستخدم `setDoc(merge)`. تمرير `undefined` **لا يمسح** القيمة القديمة (تبقى). فتفريغ العنوان لم يكن يمسحه.

### الإصلاح
- في `handleSaveEdit`: غيّرتُ `|| undefined` → `|| null` للعنوان/الفرعي/الرابط. مع `merge`، تمرير `null` **يمسح الحقل فعلياً**.
- في `lib/cms/types.ts`: وسّعتُ النوع `title/subtitle/link` ليقبل `string | null` (حتى يمرّ TypeScript).

النتيجة: تفريغ أي حقل نصّي في التعديل → يُمسح فعلياً.

> أماكن العرض كلها تستخدم `title && ...` و`|| "بنر"` — آمنة تماماً مع null.

---

## ✅ آمن

- التعديلات في **نافذة التعديل فقط** (لم ألمس الإضافة)
- `addBanner` (الإضافة) يبقى `|| undefined` — سليم (حقل جديد، لا مسح)
- العرض آمن مع null (carousel + admin list يفحصان falsy)
- متوازن

---

## الملفات المعدّلة (2)

| الملف | التغيير |
|---|---|
| `app/admin/content/homepage/page.tsx` | حقل رفع صورة في التعديل + مسح بـnull |
| `lib/cms/types.ts` | title/subtitle/link تقبل null |

## النشر

```bash
git add "app/admin/content/homepage/page.tsx" lib/cms/types.ts
git commit -m "feat(admin): edit banner image + clear text fields"
git push
```

## الاختبار
1. بانر موجود → ✏️ → ✅ تظهر صورته الحالية
2. اختاري صورة جديدة → احفظي → ✅ تتحدّث الصورة
3. امسحي العنوان (فرّغيه) → احفظي → ✅ يُمسح فعلياً
4. لا تختاري صورة جديدة → ✅ الصورة الحالية تبقى

---

## نتيجة Build / TypeScript / ESLint — بصراحة

⚠️ **لا أستطيع تشغيلها** (بيئتي بلا شبكة/تبعيات). **لن أدّعي نجاحاً.**

تحققتُ يدوياً:
- ✅ الملفان متوازنان
- ✅ handleEditFile + رفع newImageUrl + تحديث imageUrl
- ✅ مسح بـnull + النوع يقبل null (TypeScript يمرّ)
- ✅ العرض آمن مع null (فحص falsy في كل مكان)
- ✅ الإضافة لم تتأثر

**شغّلي `npm run build` محلياً وأرسلي أي خطأ.**

---

## المشاكل المتبقية (صراحة)

1. **الصورة القديمة تبقى في Storage** عند رفع صورة جديدة في التعديل (لا تُحذف). لا يكسر شيئاً لكن يراكم ملفات. تحسين لاحق: حذف القديمة عند الاستبدال.

2. **mobileImageUrl في التعديل**: نافذة التعديل تغيّر الصورة الرئيسية فقط، لا صورة الجوال. لو أردتِ تعديل صورة الجوال أيضاً، أضيفه لاحقاً.

3. **اختبار فعلي** يجب أن تجريه أنتِ.
