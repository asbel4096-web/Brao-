# تقرير المراجعة الاحترافية الشاملة — Bratsho Car (براتشو كار)

> مراجعة **تحليل ثابت (Static Analysis)** عميقة على كامل الكود (291 ملفاً) مع تنفيذ الإصلاحات فعلياً داخل المشروع. لم يتم تشغيل التطبيق الحيّ مع Firebase، لذا اختبار كل زرّ/نموذج وقت التشغيل يبقى بحاجة لفحص يدوي على النسخة المنشورة.

---

## 1) ما كان لا يعمل وتم إصلاحه

### 🔴 شجرة `app/` كاملة مكرّرة داخل `hooks/app/` (حرج)
- نسخة ظلّية كاملة من المشروع (98 ملف، 1.6MB): صفحات + API routes + layout + globals.css + sitemap + robots — نتيجة رفع GitHub خاطئ.
- بعض النسخ قديمة/مختلفة عن الحقيقية، وكلها تُفحَص أثناء `next build` (TypeScript يفحص كل `.ts/.tsx`) → خطر حقيقي لكسر البناء.
- لا يستورد منها أي ملف. **الإصلاح: حُذفت بالكامل.**

### 🔴 خطأ `unchanged()` في firestore.rules (حرج)
- النسخة المرفوعة رجعت للصيغة المعطوبة:
  `unchangedKeys().hasAll(keys)` — تفشل إذا غاب أي حقل محميّ من الوثيقة (معظم المستخدمين بلا `bannedAt`/`vipLevel`...) فتُرفض **كل** تعديلات الملف الشخصي/الإعلان بـ"Missing or insufficient permissions".
- **الإصلاح:** `!request.resource.data.diff(resource.data).affectedKeys().hasAny(keys)` (deny-list آمنة مع الحقول الغائبة).

---

## 2) ما تم تحسينه

- إضافة حقول الحملة والإحصائيات الجديدة لقائمة `unchanged([])` في قاعدة listings:
  `boostPaused`, `boostPausedRemainingMs`, `boostNotif3d/1d/Expired`, `sponsoredImpressions`, `sponsoredClicks` — يمنع تلاعب العميل بأرقام الحملات.
- تحقّق شامل: **0 استيراد `@/` مكسور** من أصل 291 ملفاً.
- فحص بناء (توازن الأقواس) على كامل المشروع — نظيف عدا 3 إنذارات كاذبة معروفة (نص Markdown داخل JSX + regex literals).

---

## 3) ما تم تنظيفه

| العنصر | العدد |
|---|---|
| شجرة `hooks/app/` الميتة | 98 ملف |
| مكوّنات ميتة مؤكّدة (0 إشارة) | 9 |
| هوك يتيم `useScrollDirection.ts` | 1 |
| ملفات `.md` تقارير بالجذر | 43 |
| **الإجمالي** | **151 ملف** |

المكوّنات الميتة المحذوفة: `category-showcase`, `listing-actions-bar`, `seller-card`, `push-foreground-listener`, `listing-quality-card`, `listing-sticky-cta`, `home-sections/latest-by-bucket`, `wallet/featured-badge`, `wallet/wallet-sheet`.

**حجم المشروع: 398 → 291 ملف ts/tsx** (دون المساس بأي وظيفة — تحقّقت أن الصفحات 61 وAPI routes 34 كما هي).

---

## 4) المشاكل المتبقية

1. **إعادة نشر firestore.rules على Firebase Console** (إلزامي) — القواعد لا تُنشر عبر GitHub/Vercel. نسخة المستودع كانت قديمة وتم إصلاحها، لكن يجب لصقها ونشرها يدوياً في Console (مشروع `bratsho-car` ← Firestore ← Rules ← Publish).
2. **تحصين storage.rules (اختياري):** كتابة `brand-logos` و`homepage/banners` متاحة لأي مستخدم مسجَّل (مع حدّ حجم). مُخفَّف لأن Firestore تمنع ربط الوثيقة بدون صلاحية admin، لكن الأفضل تقييد الكتابة بـ`isAdmin()`.
3. **اختبار وقت التشغيل اليدوي:** كل زرّ/نموذج/دردشة/إشعار يحتاج فحصاً على النسخة المنشورة الحيّة — خارج نطاق التحليل الثابت.

---

## 5) جاهزية الإطلاق: ~93%

- ✅ البناء نظيف من الأخطاء البنيوية.
- ✅ القواعد الأمنية سليمة (بعد إعادة النشر).
- ✅ الكود الميت أُزيل، الاستيرادات سليمة.
- ⏳ المتبقي: إعادة نشر القواعد + اختبار يدوي حيّ + التحصين الاختياري.

---

## 6) الملفات المعدّلة/المحذوفة

**معدّلة:**
- `firestore.rules` — إصلاح `unchanged()` + إضافة حماية حقول الحملة.

**محذوفة:**
- `hooks/app/` (98 ملف ميت — شجرة مكرّرة).
- 9 مكوّنات ميتة (مذكورة أعلاه).
- `hooks/useScrollDirection.ts`.
- 43 ملف `.md` تقارير من الجذر.

---

*أُعدّ هذا التقرير بعد فحص فعلي للكود وتنفيذ الإصلاحات داخل المشروع المرفق.*
