# ⚡ تحسين أداء القصص (Stories)

## التشخيص

فحصتُ نظام القصص بالكامل. الكود الحالي **متطوّر فعلاً** (muted autoplay، playsInline، معالجة حالات loading/ready/error). المشكلة لم تكن في جودة الكود، بل في سببين:

1. **لا preload للقصة التالية** ← أكبر سبب للبطء المحسوس. عند الانتقال، يبدأ التحميل من الصفر.
2. **لا poster للفيديو** ← شاشة سوداء أثناء تحميل الفيديو (تجربة غير احترافية).

## ما أصلحته (`story-viewer.tsx`)

### 1. Preload استباقي ⭐ (أكبر تحسين)
بمجرد فتح قصة، أحمّل **وسائط الصفحتين التاليتين** في الخلفية:
- الصور → تحميل كامل مسبق
- الفيديو → `preload="metadata"` (بدء سريع دون تحميل كامل = توفير بيانات)
- الصور المصغّرة (thumbnails) → تظهر فوراً عند الانتقال

**النتيجة**: الانتقال بين القصص يصبح فورياً تقريباً (مثل Instagram).

### 2. Poster للفيديو
أضفتُ `poster={thumbnailUrl}` — الصورة المصغّرة تظهر **فوراً** أثناء تحميل الفيديو، بدل الشاشة السوداء.

### 3. تنظيف ذكي (توفير بيانات)
عند مغادرة قصة، أوقف تحميل الفيديوهات المُجهّزة مسبقاً وغير المستخدمة — يمنع استهلاك البيانات الزائد.

## ✅ آمن

- منطق التشغيل الأصلي **لم يُلمس** (14 مرجع: autoPlay، playsInline، muted، play، videoStatus)
- التحسينات **إضافية** (preload effect جديد + poster attribute)
- لا تغيير في بنية القصص أو الرفع

---

## الملف المعدّل (1)

`components/stories/story-viewer.tsx`

## النشر

```bash
git add components/stories/story-viewer.tsx
git commit -m "perf: stories preload + video poster (Instagram-like)"
git push
```

## الاختبار
1. افتحي قصة فيها عدة صور/فيديوهات → ✅ الانتقال للتالية أسرع بكثير
2. افتحي قصة فيديو → ✅ تظهر صورة مصغّرة فوراً (لا شاشة سوداء)
3. الفيديو يبدأ تلقائياً (كان يعمل) → ✅
4. راقبي استهلاك البيانات → ✅ معقول (metadata فقط للفيديو المُجهّز)

---

## ⚠️ صراحة: ما لم يُحلّ بعد (يحتاج عملاً إضافياً)

### ضغط الفيديو عند الرفع
الصور تُضغط (`compressImage` موجود في lib/dealer/storage.ts)، **لكن الفيديو لا يُضغط**. لو الفيديوهات المرفوعة ضخمة (10MB+)، التحميل الأول يبقى بطيئاً رغم الـpreload.

**لماذا لم أحلّه الآن**: ضغط الفيديو في المتصفح صعب (يحتاج مكتبات ثقيلة مثل ffmpeg.wasm ~25MB، تبطئ التطبيق). الحلول العملية:
- **(أ) حد أقصى للحجم/المدة** عند الرفع (مثلاً 15 ثانية / 10MB) — بسيط وفعّال
- **(ب)** ضغط server-side عبر Cloud Function (الأفضل لكن يحتاج إعداد)
- **(ج)** ffmpeg.wasm في المتصفح (ثقيل، لا أنصح)

أنصح بالخيار (أ). أخبريني وأضيف حد الحجم/المدة في رسالة منفصلة.

### Cache بين الجلسات
الـpreload يعمل داخل الجلسة. لـcache بين الجلسات (فتح نفس القصة مرتين)، المتصفح يخزّن تلقائياً عبر `minimumCacheTTL` في next.config (موجود). لو أردتِ Service Worker cache أقوى، جلسة منفصلة.

---

## ملاحظة صريحة

لم أشغّل build فعلياً (لا شبكة). تحققتُ يدوياً: متوازن ✅، useEffect مستورد ✅، منطق التشغيل سليم ✅، التحسينات إضافية لا تكسر شيئاً. الـpreload يحسّن السرعة المحسوسة فوراً؛ ضغط الفيديو (لو لزم) خطوة لاحقة. أرسلي أي خطأ.
