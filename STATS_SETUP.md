# 📊 تحسينات البطاقة + نظام تتبّع الإحصائيات الآمن

تنفيذ كل النقاط المطلوبة: تصميم محسّن + إخفاء المشاهدات عن العامة + تتبّع حقيقي آمن للأحداث + لوحة إحصائيات المالك.

---

## الملفات (7)

### جديدة
| الملف | الدور |
|---|---|
| `app/api/listings/track/route.ts` | **API التتبّع الآمن** (Admin SDK، يكتب العدّادات) |
| `lib/track-event.ts` | مساعد العميل لاستدعاء الـAPI |
| `firestore/listings-counters-rules.txt` | قواعد منع التلاعب |

### معدّلة
| الملف | التغيير |
|---|---|
| `components/listing-card.tsx` | تصميم محسّن + تتبّع + إخفاء المشاهدات |
| `app/(public)/listings/[id]/page.tsx` | مشاهدة عبر API آمن (بدل كتابة مباشرة) |
| `hooks/useOwnerStats.ts` | إضافة `shareClicks` |
| `components/owner-stats-bar.tsx` | إضافة مقياس "المشاركات" |

---

## 🔒 آلية الأمان (الأهم)

```
العميل (نقرة)
   │
   ▼
trackEvent(listingId, event)   ← lib/track-event.ts
   │  (يُرفق توكن المستخدم لو مسجّل + guestKey للزائر)
   ▼
POST /api/listings/track       ← Admin SDK
   │
   ├─ يتحقق: هل الناقر هو المالك؟ → لو نعم، يتجاهل (لا يُحتسب)
   ├─ "view": dedup عبر listings/{id}/viewers/{key} → مرّة واحدة لكل شخص
   └─ يزيد العدّاد atomically (FieldValue.increment)
```

**لماذا آمن؟**
- العميل **لا يكتب** العدّادات مباشرة — فقط يطلب من الـAPI
- الـAPI يستخدم Admin SDK (يتجاوز القواعد) → العميل ممنوع من الكتابة عبر rules
- نقرات المالك على إعلانه **لا تُحتسب**
- المشاهدات **مرّة واحدة** لكل مستخدم/زائر (dedup)

---

## التحسينات على البطاقة (النقاط 1-5، 8)

1. ✅ **الصورة أكبر** (aspect-4:3 + معلومات مضغوطة = ~62% للصورة)
2. ✅ **شارة مميز/ممول/جديد** أعلى الصورة
3. ✅ **مواصفات أكبر** (أيقونات 14px، نص 12px): 📅 سنة • ⛽ وقود • ⚙️ ناقل • 📍 مدينة
4. ✅ **شارة توثيق**: "✓ تاجر موثق" أو "✓ حساب موثق"
5. ✅ **وقت النشر**: 🕒 منذ ساعتين
8. ✅ **مظهر محسّن**: مسافات أكبر، ظلال أنعم، أقل ازدحام

## إخفاء المشاهدات (النقطة 6)

- ✅ البطاقة: **لا تعرض المشاهدات إطلاقاً** للعامة
- ✅ صفحة التفاصيل: المشاهدات داخل `OwnerStatsBar` المحمي بـ`OwnerOnly` (المالك فقط — كان موجوداً وبقي)
- ✅ المشاهدات تظهر فقط في "إعلاناتي" + لوحة المالك

## لوحة المالك (النقطة 7)

`OwnerStatsBar` (موجود + محسّن) يعرض 6 مقاييس للمالك فقط:
👁️ مشاهدات · ❤️ مفضلة · 💬 نقرات الدردشة · 📞 نقرات الاتصال · 🟢 نقرات واتساب · 🔗 مشاركات

## التوافق (النقطة 4)

- ✅ كل عدّاد غير موجود = 0 تلقائياً (الإعلانات القديمة تعمل)
- ✅ كل حقل يُعرض فقط إن وُجد

---

## النشر

```bash
git add app/api/listings/track lib/track-event.ts \
        components/listing-card.tsx components/owner-stats-bar.tsx \
        hooks/useOwnerStats.ts \
        "app/(public)/listings/[id]/page.tsx"
git commit -m "feat: secure event tracking + card redesign + hide public views"
git push
```

### ⚠️ Firestore Rules (مهم للأمان الكامل)

راجعي `firestore/listings-counters-rules.txt`. أضيفي شرط `affectedKeys` لقاعدة `listings` update لمنع المالك من تعديل عدّاداته يدوياً، وأضيفي قاعدة `viewers` (write: false). بدونها، النظام يعمل لكن مالك متمرّس قد يعدّل أرقامه.

---

## الاختبار

### التتبّع:
1. افتحي إعلاناً **لشخص آخر** → المشاهدة تُسجَّل مرّة واحدة
2. أعيدي الفتح → ✅ لا تزداد (dedup)
3. افتحي إعلانك أنتِ → ✅ لا تُحتسب مشاهدة
4. اضغطي اتصال/واتساب/مراسلة على إعلان غيرك → تُحتسب
5. ادخلي "إعلاناتي" → ✅ ترين الأرقام تكبر

### الخصوصية:
1. كزائر، افتحي أي إعلان → ✅ لا ترى عدد مشاهدات
2. في البطاقات → ✅ لا مشاهدات
3. كمالك في "إعلاناتي" → ✅ ترى كل الإحصائيات

### التصميم:
- ✅ صورة أكبر، مواصفات أوضح، شارة توثيق، مظهر أنظف
