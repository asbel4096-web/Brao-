# 🚗 حقول جديدة: حالة السيارة + نوع الدفع

إضافة حقلين جديدين عبر كامل المنصة: `vehicleCondition` (جديدة/مستعملة) و `driveType` (أمامي/خلفي/رباعي).

## الملفات المعدّلة (6)

| # | الملف | التغيير |
|---|---|---|
| 1 | `lib/types.ts` | أضيف `vehicleCondition?` و `driveType?` لـ`Listing` + `driveType?` لـ`SearchFilters` |
| 2 | `lib/categories.ts` | أضيف `vehicleConditions` و `driveTypes` |
| 3 | `app/(public)/add-listing/page.tsx` | حقلا إدخال + حفظ في Firestore (للسيارات فقط) |
| 4 | `app/(public)/listings/page.tsx` | 6 فلاتر جديدة + ChipRow + chips نشطة |
| 5 | `app/(public)/listings/[id]/page.tsx` | عرض الحالة + نوع الدفع في المواصفات |
| 6 | `components/listing-card.tsx` | شارة الحالة + الوقود + نوع الدفع |

---

## النقاط الثماني (كلها منفّذة)

1. ✅ **add-listing**: حقلا "حالة السيارة" و"نوع الدفع" (select) بعد ناقل الحركة
2. ✅ **حفظ Firestore**: يُحفظان فقط لأقسام السيارات (`showVehicleSpecs`) — توافق مع الورش/القطع
3. ✅ **Types**: محدّثة في `Listing` و `SearchFilters`
4. ✅ **Schema**: القوائم في `categories.ts`
5. ✅ **البحث والفلاتر**: السعر + المدينة + سنة الصنع + الوقود + الناقل + الحالة + الدفع
6. ✅ **Queries**: الفلترة client-side على الإعلانات المُحمّلة (لا index جديد مطلوب)
7. ✅ **التوافق**: الإعلانات القديمة بلا الحقول لا تُستبعد إلا عند تفعيل الفلتر صراحة
8. ✅ **العرض**: بطاقة الإعلان + صفحة التفاصيل

---

## كيف تعمل الفلاتر الجديدة

أضفتُ مكوّن **ChipRow** — صف chips للاختيار الأحادي مع خيار "الكل". الفلاتر:
- **سنة الصنع**: حقلا "من/إلى" (نطاق)
- **نوع الوقود**: chips (بنزين/ديزل/هايبرد/كهرباء/غاز)
- **ناقل الحركة**: chips (أوتوماتيك/عادي/نصف أوتوماتيك)
- **حالة السيارة**: chips (جديدة/مستعملة)
- **نوع الدفع**: chips (أمامي/خلفي/رباعي)

كلها تظهر في الـbottom sheet، وتُضاف كـchips نشطة فوق النتائج، وتُمسح بزر "إعادة تعيين".

---

## التوافق مع الإعلانات القديمة (مهم)

الإعلانات المنشورة **قبل** هذا التحديث لا تحوي `vehicleCondition` ولا `driveType`. السلوك:
- **بدون تفعيل الفلتر**: تظهر طبيعياً (لا استبعاد)
- **عند تفعيل فلتر الحالة/الدفع**: تُستبعد فقط الإعلانات التي لا تطابق (طبيعي)
- **في البطاقة/التفاصيل**: الحقول الجديدة تظهر فقط إن وُجدت (شرط `&&`)

لا حاجة لـmigration. الإعلانات الجديدة ستحوي الحقول تلقائياً.

---

## النشر

```bash
git add lib/types.ts lib/categories.ts \
        "app/(public)/add-listing/page.tsx" \
        "app/(public)/listings/page.tsx" \
        "app/(public)/listings/[id]/page.tsx" \
        components/listing-card.tsx
git commit -m "feat: add vehicleCondition + driveType across listings"
git push
```

✅ لا rules جديدة (الكتابة عبر add-listing مسموحة بالفعل).
✅ لا index جديد (الفلترة client-side).
✅ لا dependencies جديدة.

---

## الاختبار

### إضافة إعلان:
1. `/add-listing` → اختاري قسم "سيارات"
2. في خطوة المواصفات: سترين "حالة السيارة" و"نوع الدفع"
3. انشري → ✅ يُحفظان في Firestore

### الفلاتر:
1. `/listings` → اضغطي "فلاتر"
2. ✅ سترين: سنة الصنع، نوع الوقود، ناقل الحركة، حالة السيارة، نوع الدفع
3. اختاري "جديدة" + "رباعي" → "عرض النتائج"
4. ✅ تُفلتر النتائج + chips نشطة تظهر فوق

### العرض:
1. بطاقة الإعلان: ✅ شارة الحالة + الوقود + الدفع
2. صفحة التفاصيل: ✅ الحالة + نوع الدفع في المواصفات

### التوافق:
- إعلان قديم بلا الحقول → ✅ يظهر طبيعياً، بلا شارات حالة/دفع
