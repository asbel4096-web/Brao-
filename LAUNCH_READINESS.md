# 🚀 تقرير جاهزية الإطلاق — براتشو كار

تدقيق شامل قبل النشر الرسمي. مرتّب حسب الأولوية.

---

## 🔴 حرج — يجب قبل الإطلاق

### 1. نشر الفهارس في Firebase Console
`firestore.indexes.json` يحوي 15 فهرساً صحيحاً (status+views، status+featured، status+isFeatured، status+brand، status+createdAt...)، **لكن وجودها في الملف ≠ نشرها**. الفهارس تُنشر فقط عبر:

```bash
firebase deploy --only firestore:indexes
```
أو: افتحي الرئيسية + DevTools Console → اضغطي روابط "create index" التي تظهر عند فشل أي استعلام.

**بدون نشرها**: أقسام "الأكثر مشاهدة" و"المميزة" و"عدد الماركات" تفشل بصمت (تظهر فارغة).

**تحقّق**: Firebase Console → Firestore → Indexes → تأكدي أن كل الفهارس حالتها "Enabled".

### 2. متغيّرات البيئة على Vercel
`.env.example` (مرفق) يوضّح كل المتغيّرات المطلوبة. تأكدي أن **كلها مضبوطة** في Vercel → Settings → Environment Variables، خاصةً:
- `NEXT_PUBLIC_FIREBASE_*` (7 متغيّرات) — بدونها التطبيق لا يتصل بـFirebase
- `FIREBASE_ADMIN_*` (3) — لوظائف الخادم/الكرون
- `CRON_SECRET` — لحماية كرون تنظيف العروض
- `NEXT_PUBLIC_SITE_URL` — للروابط/SEO

### 3. نشر قواعد الأمان
`firestore.rules` + `storage.rules` موجودان، لكن تأكدي أنهما **منشوران** (مشكلة تكرّرت سابقاً - القواعد في الملف تتأخّر عن Console):
```bash
firebase deploy --only firestore:rules,storage
```
**تحقّق**: Firebase Console → Firestore → Rules → قارني التاريخ.

---

## 🟡 مهم — يُفضّل قبل الإطلاق

### 4. اختبار نشر إعلان من كل قسم
بعد إدماج DynamicFields، اختبري فعلياً:
- نشر سيارة → يُحفظ ✅
- نشر قطعة غيار → الحالة + السيارة المتوافقة تُحفظ ✅
- نشر خدمة/ورشة → التقييم يُحفظ ✅
- تأكدي من ظهورها في أقسامها الصحيحة بالرئيسية

### 5. التحقق من الفهرس المركّب لـlatest-by-bucket
`latest-by-bucket` يستعلم `status + orderBy(createdAt)` — يحتاج فهرس `status+createdAt` (✅ موجود في الملف، تأكدي أنه منشور).

### 6. أداء الرئيسية (قراءات Firestore)
عند أول تحميل، الرئيسية تقرأ: المميزة + 4 أقسام أحدث (~240) + الأكثر حفظاً (~60) + إحصائيات (5 count) + عدد الماركات (8 count). **مقبول** لأن كله مُخزّن (cache)، لكن راقبي فاتورة Firestore أول أسبوع. لو ارتفعت، نحسّن بفهارس مخصّصة لكل دلو.

---

## 🟢 اختياري — تحسينات لاحقة

### 7. دمج قسم "الأكثر مشاهدة"
`MostViewedSection` (سلّمته في homev3) **غير مدمج** في الرئيسية حالياً. لو أردتِه، أدمجه في رسالة لاحقة.

### 8. ListingsGrid الاحتياطي
`components/listings-grid.tsx` ما زال موجوداً (غير مستخدم بعد المرحلة 5). آمن - يمكن إبقاؤه أو حذفه لاحقاً.

### 9. صفحات تفاصيل: تخطيطات متقدّمة لكل قسم
حالياً DynamicSpecs يعرض المواصفات الصحيحة. لو أردتِ تخطيطاً مختلفاً جذرياً لكل قسم (لا مجرد مواصفات)، جلسة منفصلة.

---

## ✅ ما هو جاهز فعلاً

- ✅ نظام الأقسام الديناميكي كامل (المراحل 1-6)
- ✅ TypeScript strict mode مفعّل
- ✅ قواعد الأمان + الكرون موجودة
- ✅ الإعلانات المميزة تعمل (مؤكّد بصورة)
- ✅ الإحصائيات الحقيقية تعمل
- ✅ الشريط السفلي المصغّر
- ✅ إصلاح تكرار "د.ل"
- ✅ لا console.log منسية في الكود الجديد
- ✅ الاستيرادات سليمة، الملفات متوازنة

---

## خلاصة الأولويات

| # | المهمة | الأولوية | كيف |
|---|---|---|---|
| 1 | نشر الفهارس | 🔴 حرج | `firebase deploy --only firestore:indexes` |
| 2 | متغيّرات Vercel | 🔴 حرج | Vercel Settings (راجعي .env.example) |
| 3 | نشر القواعد | 🔴 حرج | `firebase deploy --only firestore:rules,storage` |
| 4 | اختبار النشر | 🟡 مهم | نشر إعلان من 3 أقسام |
| 5-6 | فحص الأداء | 🟡 مهم | راقبي Firestore أسبوعاً |
| 7-9 | تحسينات | 🟢 اختياري | جلسات لاحقة |

---

## الخطوة التالية المقترحة

ابدئي بالبنود الحرجة الثلاثة (نشر الفهارس + متغيّرات Vercel + نشر القواعد) — هذه تمنع أعطالاً صامتة عند الإطلاق. بعدها اختبري النشر من أقسام مختلفة.

> ملاحظة صريحة: لم أستطع التحقق من **نشر** الفهارس/القواعد فعلياً (لا وصول لـFirebase Console من بيئتي). تحققتُ فقط من وجودها في الملفات. التحقق النهائي من النشر مسؤوليتك عبر Console.
