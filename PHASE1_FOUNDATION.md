# 🏗️ نظام الأقسام الديناميكي — المرحلة 1: البنية الأساسية

## ما بُني

ملفان **جديدان بالكامل** يؤسّسان لنظام الأقسام الديناميكي:

### 1. `lib/category-config.ts` — مصدر الحقيقة الواحد
يعرّف **الحقول الدقيقة لكل قسم** (لا حقول موحّدة):
- **سيارات/حافلات/شاحنات**: عنوان، ماركة، موديل، سنة، محرك، ناقل، وقود، مسافة، سعر، مدينة، هاتف
- **قطع الغيار** (7 أقسام): اسم القطعة، الحالة، السيارة المتوافقة، سعر، مدينة، هاتف
- **سطحة/سحب**: اسم الخدمة، مناطق التغطية، متاح الآن، سعر، مدينة، هاتف
- **ورش/خدمات** (ميكانيكي، سمكرة، ورش، كهرباء): اسم الورشة، وصف، تقييم، مدينة، هاتف
- **سيارات بها حوادث / معارض**: حقول مخصّصة

الدوال:
- `getCategoryConfig(slugOrName)` → إعداد القسم الكامل (حقول + entityType + homeBucket)
- `getAllCategoryConfigs()` → كل الإعدادات

### 2. `lib/category-mapping.ts` — Auto Category Mapping
يربط فئة الإعلان بقسمها في الرئيسية تلقائياً:
```
سيارات → "أحدث السيارات"
قطع غيار → "أحدث قطع الغيار"
ساحبة → "أحدث السطحات"
ميكانيكي/سمكرة/ورش → "أحدث خدمات الصيانة"
```
الدوال:
- `bucketForCategoryName(name)` → دلو العرض
- `getHomeBuckets()` → كل أقسام الرئيسية المنفصلة (ديناميكياً)
- `categoryNamesForBucket(bucket)` → أسماء الفئات لاستعلام Firestore

> **أي قسم جديد يُضاف مستقبلاً لـcategories يُصنَّف تلقائياً** — لا تعديل يدوي.

---

## ✅ لماذا هذه المرحلة آمنة 100%

- الملفان **جديدان** ولا يُستوردان من أي مكان بعد → **لا يؤثران على أي صفحة قائمة**.
- مبنيان فوق `categories` الموجودة (لا يكرّرانها).
- أسماء الحقول تطابق حقول Listing في Firestore (لا يتغيّر التخزين).
- لا تعديل على `getAddListingConfig` الحالي (يبقى يعمل).

**يمكنك رفعهما بأمان تام الآن** — لن يكسرا شيئاً، وسيكونان جاهزين للمراحل القادمة.

---

## الملفات الجديدة (2)

| الملف | الدور |
|---|---|
| `lib/category-config.ts` | تعريف حقول كل قسم |
| `lib/category-mapping.ts` | ربط القسم → الرئيسية |

## النشر

```bash
git add lib/category-config.ts lib/category-mapping.ts
git commit -m "feat(phase1): dynamic category system foundation"
git push
```

---

## 🔜 المراحل القادمة (كل مرحلة مختبَرة قبل التالية)

**المرحلة 2 — النموذج الديناميكي**: `DynamicListingForm` يقرأ `getCategoryConfig` ويرسم الحقول المناسبة لكل قسم (سيارة ترى حقول السيارة، قطعة غيار ترى حقولها...). يستبدل تدريجياً النموذج الحالي مع الحفاظ على منطق الحفظ.

**المرحلة 3 — البطاقات الديناميكية**: `CarCard`, `PartsCard`, `ServiceCard`... كل قسم بطاقته. + قاعدة "لا تعرض حقلاً فارغاً".

**المرحلة 4 — أقسام الرئيسية المنفصلة**: "أحدث السيارات"، "أحدث قطع الغيار"، "أحدث السطحات"... تستهلك `getHomeBuckets`.

**المرحلة 5 — صفحات تفاصيل ديناميكية** حسب القسم.

> أخبريني عند جاهزيتك للمرحلة 2، وسأبنيها وأتحقق منها قبل المتابعة.

---

## ملاحظة صريحة

لم أشغّل `tsc`/build فعلياً (بيئتي بلا شبكة). تحققتُ يدوياً: الأقواس متوازنة ✅، كل slug يطابق الأقسام الحقيقية الـ17 ✅، الاستيرادات صحيحة (`categories`, `CategoryDef` مُصدَّران فعلاً) ✅، المنطق سليم (كل قسم في الدلو الصحيح) ✅، لا `any`. الاختبار الحقيقي عند الرفع — لو ظهر خطأ، أرسليه.
