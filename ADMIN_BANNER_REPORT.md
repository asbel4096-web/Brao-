# 📋 لوحة تحكم البانرات — دعم تواريخ العرض

أكملتُ الفجوة من المرحلة السابقة: لوحة التحكم الآن تدعم **تواريخ البدء/الانتهاء** لإدارة إخفاء البانرات تلقائياً من اللوحة (بدل Firestore يدوياً).

## ما أضفته (`app/admin/content/homepage/page.tsx`)

### حقلا تاريخ في نموذج إضافة البانر
- **تاريخ البدء** (اختياري): قبله لا يظهر البانر
- **تاريخ الانتهاء** (اختياري): بعده يُخفى البانر تلقائياً

كلاهما `<input type="date">` (يدعم كل الأجهزة). تُحفظ كـ`Timestamp` في Firestore.

### كيف يعمل (متكامل مع المرحلة السابقة)
1. الأدمن يحدّد تاريخ انتهاء عند إضافة البانر → يُحفظ كـTimestamp
2. `use-public-homepage-config` (من المرحلة السابقة) يفلتر البانرات المنتهية تلقائياً
3. النتيجة: البانر يختفي من الرئيسية بعد تاريخ انتهائه **بلا تدخّل يدوي**

---

## ✅ آمن

- التعديل **إضافي** (حقلان + تمرير للحفظ)
- لم ألمس منطق رفع الصورة/العنوان/الرابط/الترتيب
- `addBanner` يمرّر الحقول كاملة (`...data`) — يستوعبها
- قاعدة `banners` لا تقيّد الحقول (`isAdmin()`) — تُحفظ بلا تعديل قواعد
- التواريخ تُمرّر فقط لو لها قيمة (لا undefined)

---

## الملف المعدّل (1)

`app/admin/content/homepage/page.tsx`

## النشر

```bash
git add "app/admin/content/homepage/page.tsx"
git commit -m "feat(admin): banner start/end date fields"
git push
```

## الاختبار
1. لوحة التحكم → المحتوى → الصفحة الرئيسية → البنرات → إضافة بنر
2. ✅ يظهر حقلا "تاريخ البدء" + "تاريخ الانتهاء"
3. أضيفي بانراً بتاريخ انتهاء اليوم → غداً ✅ يختفي تلقائياً من الرئيسية
4. أضيفي بانراً بتاريخ بدء غداً → اليوم ✅ لا يظهر، غداً يظهر

---

## ملاحظة عن mobileImageUrl

أضفتُ `mobileImageUrl` للنوع والعرض في المرحلة السابقة، لكن لم أضف حقل رفعه في اللوحة هنا (يتطلب رفع صورة ثانية = تعقيد). البانر يعمل بصورة واحدة (`imageUrl`) لكل الأجهزة حالياً — وهذا كافٍ. لو أردتِ صورة جوال منفصلة من اللوحة، أضيف حقل رفع ثانٍ في رسالة منفصلة.

---

## نتيجة Build / TypeScript / ESLint — بصراحة

⚠️ **لا أستطيع تشغيلها** (بيئتي بلا شبكة/تبعيات). **لن أدّعي نجاحاً.**

تحققتُ يدوياً:
- ✅ الملف متوازن
- ✅ Timestamp مستورد من firebase/firestore
- ✅ startDate/endDate في draft + الحفظ + النموذج + التصفير (كل المواضع)
- ✅ Timestamp.fromDate صحيح
- ✅ التواريخ لا تُمرّر undefined
- ✅ addBanner + قاعدة banners يستوعبان الحقول

**شغّلي `npm run build` محلياً وأرسلي أي خطأ.**

---

## المشاكل المتبقية (صراحة)

1. **تعديل بانر موجود لا يحرّر التواريخ**: أضفتُ التواريخ لنموذج **الإضافة**. تعديل بانر موجود (لو كانت هناك واجهة تعديل منفصلة) قد يحتاج نفس الحقول. النموذج الحالي للإضافة فقط.

2. **mobileImageUrl** غير قابل للرفع من اللوحة (مذكور أعلاه).

3. **اختبار فعلي** يجب أن تجريه أنتِ.
